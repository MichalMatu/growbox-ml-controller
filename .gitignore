# --- OS ---
.DS_Store
Thumbs.db
Desktop.ini

# --- Editors / IDE ---
.idea/
.vscode/
*.swp
*.swo
*~
*.code-workspace

# --- Secrets & local env ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# --- Logs & caches ---
*.log
logs/
.cache/
.turbo/
.parcel-cache/
.eslintcache
.stylelintcache
*.tsbuildinfo

# --- Node / frontend (configurator app) ---
node_modules/
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.vite/
coverage/
playwright-report/
test-results/
storybook-static/
*.local

# Package manager logs/cache — this repo is pnpm-only (see AGENTS.md)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
# Do not ignore package-lock.json or yarn.lock: the gate must expose and reject them.

# --- Python (local tools only; not part of this sparse tree) ---
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.mypy_cache/

# --- Monorepo ghosts (if working tree was checked out from main) ---
# These paths are not used on this FE/docs branch; keep them untracked if present.
/tools/
/tests/
/test/
/src/
/lib/
/components/
/third_party/
/scripts/
/config/
/examples/
/build/
# Note: /gate/, root package.json, and pnpm-lock.yaml ARE tracked (root golden gate).
# Do not ignore them.
sdkconfig
sdkconfig.old
CMakeLists.txt
Makefile
pyproject.toml
requirements*.txt
.platformio/
.pio/
