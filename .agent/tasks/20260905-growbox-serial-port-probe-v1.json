{
  "id": "20260905-growbox-serial-port-probe-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": ["board:growbox-s3"],
  "memory_limit_mb": 2048,
  "command_timeout": 120,
  "idle_timeout": 60,
  "task_timeout": 300,
  "commands": [
    "set -euo pipefail; EXPECTED=dfc6dc86a47ad2158e36bb0d5241b0153dbce387; git fetch -q origin mvp/environment-controller; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; test -z \"$(git status --porcelain)\"",
    ".venv/bin/python - <<'PY'\nfrom serial.tools import list_ports\nfor p in list_ports.comports():\n    if 'usbserial' in p.device.lower() or 'ch340' in (p.description or '').lower():\n        print(f'PORT_META device={p.device} description={p.description!r} hwid={p.hwid!r} serial={p.serial_number!r}')\nPY",
    ".venv/bin/python - <<'PY'\nimport glob,time,serial\nports=sorted(set(glob.glob('/dev/cu.usbserial-*')))\nfor port in ports:\n    text=''\n    try:\n        with serial.Serial(port,115200,timeout=.08,write_timeout=1) as h:\n            try: h.dtr=False; h.rts=False\n            except Exception: pass\n            time.sleep(.5); h.reset_input_buffer(); h.write(b'status\\n'); h.flush()\n            end=time.monotonic()+2.5\n            while time.monotonic()<end:\n                b=h.read(4096)\n                if b: text += b.decode(errors='replace')\n        hits=[ln.strip() for ln in text.splitlines() if 'firmware_sha=' in ln or 'outputs=' in ln or 'rf_ready=' in ln]\n        print(f'PORT_PROBE port={port} bytes={len(text)} hits={len(hits)}')\n        for ln in hits[:8]: print('PORT_LINE '+port+' '+ln[:1000])\n    except Exception as e:\n        print(f'PORT_PROBE_ERROR port={port} error={type(e).__name__}:{e}')\nPY"
  ]
}
