{
  "id": "20260831-growbox-stage27c-tp357-raw-discovery-v2",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": true,
  "command_timeout": 2400,
  "idle_timeout": 900,
  "task_timeout": 7200,
  "memory_limit_mb": 8192,
  "commands": [
    "git fetch origin mvp/environment-controller && git reset --hard origin/mvp/environment-controller && git clean -fd && test \"$(git rev-parse HEAD)\" = \"d0b52a352d2488dba4d1f5e64af579ec8fa1545f\" && test -e /dev/cu.usbserial-110 && test -z \"$(git status --short)\"",
    "set -euo pipefail; python3 - <<'PY'\nfrom pathlib import Path\n\np = Path('src/climate/native/BleOutsideSource.cpp')\ns = p.read_text()\ns = s.replace('#include <esp_err.h>\\n', '#include <esp_err.h>\\n#include <esp_log.h>\\n')\nmarker = 'BleOutsideSource* g_instance = nullptr;\\n'\ninsert = '''BleOutsideSource* g_instance = nullptr;\nconstexpr char kTp357DebugTag[] = \"tp357_raw\";\nconstexpr std::array<std::uint8_t, 6> kTp357DebugMac{0xF7U, 0x5FU, 0x8DU, 0x0FU, 0x76U, 0x20U};\n'''\nassert marker in s and 'kTp357DebugMac' not in s\ns = s.replace(marker, insert)\nold = '''void BleOutsideSource::handleAdvertisement(const std::uint8_t* address, const std::uint8_t* data,\n                                           std::size_t size) noexcept {\n  if (!matchesNimbleAddress(target_mac_, address)) {\n'''\nnew = '''void BleOutsideSource::handleAdvertisement(const std::uint8_t* address, const std::uint8_t* data,\n                                           std::size_t size) noexcept {\n  if (matchesNimbleAddress(kTp357DebugMac, address)) {\n    static std::uint32_t logged_frames = 0U;\n    if (logged_frames < 12U && data != nullptr) {\n      constexpr char kHex[] = \"0123456789ABCDEF\";\n      char encoded[161]{};\n      const std::size_t clipped = size < 80U ? size : 80U;\n      for (std::size_t i = 0U; i < clipped; ++i) {\n        encoded[i * 2U] = kHex[(data[i] >> 4U) & 0x0FU];\n        encoded[i * 2U + 1U] = kHex[data[i] & 0x0FU];\n      }\n      encoded[clipped * 2U] = static_cast<char>(0);\n      ESP_LOGI(kTp357DebugTag, \"frame=%u len=%u data=%s\",\n               static_cast<unsigned>(++logged_frames), static_cast<unsigned>(size), encoded);\n    }\n  }\n\n  if (!matchesNimbleAddress(target_mac_, address)) {\n'''\nassert old in s\np.write_text(s.replace(old, new))\nPY\nrc=0; .venv/bin/pre-commit run clang-format --files src/climate/native/BleOutsideSource.cpp || rc=$?; test \"$rc\" -le 1; .venv/bin/pre-commit run --files src/climate/native/BleOutsideSource.cpp; git diff --check; grep -q 'kTp357DebugMac' src/climate/native/BleOutsideSource.cpp; ! git diff --numstat | grep -q '^-'",
    "bash scripts/stage27c_crowpanel.sh clean && bash scripts/stage27c_crowpanel.sh build",
    "set -uo pipefail; base=d0b52a352d2488dba4d1f5e64af579ec8fa1545f; cleanup(){ git reset --hard \"$base\" >/dev/null && git clean -fd >/dev/null; }; trap cleanup EXIT; rc=0; PORT=/dev/cu.usbserial-110 bash scripts/stage27c_crowpanel.sh flash || rc=$?; if [ \"$rc\" -eq 0 ]; then .venv/bin/python - <<'PY' | tee /tmp/stage27c-tp357-raw-v2.log\nimport serial\nimport time\n\nport = '/dev/cu.usbserial-110'\ndeadline = time.monotonic() + 180.0\nprint(f'SERIAL_CAPTURE_BEGIN port={port} seconds=180', flush=True)\nwith serial.Serial(port, 115200, timeout=1.0) as ser:\n    while time.monotonic() < deadline:\n        raw = ser.readline()\n        if raw:\n            line = raw.decode('utf-8', errors='replace').rstrip()\n            if 'tp357_raw' in line or 'climate_stage27' in line or 'scd41' in line:\n                print(line, flush=True)\nprint('SERIAL_CAPTURE_END', flush=True)\nPY\n  grep -q 'tp357_raw:' /tmp/stage27c-tp357-raw-v2.log || rc=20; grep -q 'Stage27 real-input runtime: i2c=1 scd41=1 ds3231=1 ble=1 outputs=fake-locked' /tmp/stage27c-tp357-raw-v2.log || rc=21; grep -q 'scd41_sample=1' /tmp/stage27c-tp357-raw-v2.log || rc=22; grep -q 'rtc_available=1 rtc_trusted=1' /tmp/stage27c-tp357-raw-v2.log || rc=23; fi; cleanup || rc=24; trap - EXIT; test \"$(git rev-parse HEAD)\" = \"$base\" || rc=25; test -z \"$(git status --short)\" || rc=26; exit \"$rc\""
  ]
}
