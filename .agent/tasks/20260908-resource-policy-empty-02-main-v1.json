{
  "id": "20260908-resource-policy-empty-02-main-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "main",
  "allow_write": true,
  "resources": [],
  "memory_limit_mb": 512,
  "command_timeout": 300,
  "idle_timeout": 120,
  "task_timeout": 900,
  "commands": [
    "set -euo pipefail; test -z \"$(git status --porcelain)\"; python3 - <<'PY'\nfrom pathlib import Path\np=Path('AGENTS.md'); s=p.read_text()\nold='Every task must declare `resources` explicitly. Missing or invalid declarations are terminal task-contract errors; there is no compatibility fallback to `machine`. Use `resources: []` for repository-local software work, including builds/tests, when no exclusive external device or host-global state is used. `memory_limit_mb` is an independent RSS watchdog and does not determine resource classification. Use stable named resources such as `board:growbox-s3` for USB/serial/flash/monitor/hardware work so only tasks sharing that concrete resource serialize. Use `resources: [\\\"machine\\\"]` only for genuine whole-host operations such as global Local Agent maintenance or host-global toolchain mutation. Resource contention is a wait state: the immutable task remains pending and is retried.'\nnew='Every task must declare `resources` explicitly. Missing or invalid declarations are terminal task-contract errors; there is no compatibility fallback. Repository policy is simple: every executable task uses `resources: []`, including builds/tests and USB/serial/flash/monitor/hardware work. Repository lease provides per-project serialization; detect and verify the current device/port inside the task instead of reserving it as a scheduler resource. Do not declare named resources or `machine` from this repository; host-global Local Agent maintenance belongs to the supervisor/administration path. `memory_limit_mb` remains an independent RSS watchdog.'\nassert s.count(old)==1,s.count(old); p.write_text(s.replace(old,new))\nPY\ngit diff --check; ! grep -n -E 'board:growbox-s3|resources: \\\[\\\"machine\\\"\\\]' AGENTS.md; git diff -- AGENTS.md",
    "set -euo pipefail; git add AGENTS.md; git commit -m 'Simplify Local Agent resource policy'; git push origin HEAD:main"
  ]
}
