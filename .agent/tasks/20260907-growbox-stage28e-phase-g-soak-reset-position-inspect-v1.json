{
  "id": "20260907-growbox-stage28e-phase-g-soak-reset-position-inspect-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": [],
  "memory_limit_mb": 4096,
  "command_timeout": 1200,
  "idle_timeout": 300,
  "task_timeout": 1800,
  "commands": [
    "set -euo pipefail; EXPECTED=389453882f0e0d2209c5bdece7eaf443895aa7ba; git fetch -q origin mvp/environment-controller agent-control; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; test -z \"$(git status --porcelain)\"; echo STAGE28E_G_SOAK_RESET_POSITION_EXACT_SHA_PASS=$EXPECTED",
    "set -euo pipefail; git show origin/agent-control:.agent/results/20260907-growbox-stage28e-phase-g-long-soak-v1.json > /tmp/g-soak.json; python3 - <<'PY'\nimport json,re\nd=json.load(open('/tmp/g-soak.json'))\nout=d['commands'][1].get('output','')\nlines=out.splitlines()\nprint('OUTPUT_CHARS',len(out),'LINES',len(lines),'TRUNCATED',d['commands'][1].get('output_truncated'))\nidx=[i for i,x in enumerate(lines) if 'ESP-ROM:esp32s3-' in x]\nprint('ESP_ROM_LINE_INDEXES',idx)\nfor i in idx:\n    print('--- ESP CONTEXT',i,'---')\n    for j in range(max(0,i-8),min(len(lines),i+20)):\n        print(f'{j}: {lines[j]}')\nlife=[(i,x) for i,x in enumerate(lines) if 'stage28e_runtime_lifecycle entry_count=' in x or 'stage28e_lifecycle: arbiter_construct ' in x]\nprint('LIFECYCLE_MARKERS',len(life))\nfor i,x in life: print('LIFE',i,x)\nstatus=[]\nfor i,x in enumerate(lines):\n    if x.startswith('status firmware_sha='):\n        vals=dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',x)); status.append((i,vals))\nprint('STATUS_COUNT',len(status))\nfor i,v in status: print('STATUS',i,'boot_id='+str(v.get('boot_id')),'reset_reason='+str(v.get('reset_reason')),'uptime_ms='+str(v.get('uptime_ms')),'outputs='+str(v.get('outputs')))\ntele=[]\nfor i,x in enumerate(lines):\n    if 'soak_v=2 firmware_sha=389453882f0e0d2209c5bdece7eaf443895aa7ba' in x and 'storage_queue_drops=' in x:\n        vals=dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',x)); tele.append((i,vals))\nprint('TELE_COUNT',len(tele))\nif tele:\n    print('TELE_FIRST',tele[0][0],tele[0][1].get('uptime_ms'),tele[0][1].get('reset_reason'),tele[0][1].get('outputs'))\n    print('TELE_LAST',tele[-1][0],tele[-1][1].get('uptime_ms'),tele[-1][1].get('reset_reason'),tele[-1][1].get('outputs'))\nprint('LAST_LINE',lines[-1] if lines else '')\nprint('STAGE28E_G_SOAK_RESET_POSITION_INSPECT_PASS')\nPY",
    "set -euo pipefail; test -z \"$(git status --porcelain)\"; git diff --check; echo STAGE28E_G_SOAK_RESET_POSITION_GATE_PASS"
  ]
}