{
  "id": "20260907-growbox-stage28e-phase-e1-queue-psram-hardware-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": ["board:growbox-s3"],
  "memory_limit_mb": 6144,
  "command_timeout": 7200,
  "idle_timeout": 900,
  "task_timeout": 10800,
  "commands": [
    "set -euo pipefail; EXPECTED=4a5121abf2a93ba76bfc219575d2b52b8025fb03; git fetch -q origin mvp/environment-controller; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; test -z \"$(git status --porcelain)\"; test -e /dev/cu.usbserial-1130; git diff --check; echo STAGE28E_E1_HW_EXACT_SHA_PASS=$EXPECTED",
    "set -euo pipefail; export CMAKE_BUILD_PARALLEL_LEVEL=1; export STAGE27C_BUILD_DIR=build/idf-stage28e-e1-queue-psram-hw; export GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=0; export GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0; export GROWBOX_RF433_LOOPBACK_ENABLED=0; export GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0; export GROWBOX_FIRMWARE_GIT_SHA=4a5121abf2a93ba76bfc219575d2b52b8025fb03; bash scripts/stage27c_crowpanel.sh build >/tmp/stage28e-e1-hw-build.log 2>&1; PORT=/dev/cu.usbserial-1130 bash scripts/stage27c_crowpanel.sh flash >/tmp/stage28e-e1-hw-flash.log 2>&1; echo STAGE28E_E1_HW_FLASH_PASS",
    ".venv/bin/python - <<'PY'\nimport json,re,statistics,time,urllib.request\nimport serial\np='/dev/cu.usbserial-1130'\nh=serial.Serial(p,115200,timeout=0.1,write_timeout=1); h.dtr=False; h.rts=False\nstart=time.monotonic(); end=start+105; raw=bytearray(); sent=set()\nschedule=[(18,b'status\\n','s18'),(50,b'status\\n','s50'),(92,b'status\\n','s92')]\nwhile time.monotonic()<end:\n    elapsed=time.monotonic()-start\n    for at,cmd,name in schedule:\n        if elapsed>=at and name not in sent:\n            h.write(cmd); h.flush(); sent.add(name)\n    n=h.in_waiting\n    if n: raw.extend(h.read(n))\n    time.sleep(0.05)\ntime.sleep(1); n=h.in_waiting\nif n: raw.extend(h.read(n))\nh.close(); text=raw.decode(errors='replace')\nprint('STAGE28E_E1_HW_SERIAL_CHARS',len(text)); print(text[-46000:])\nif 'queue_psram=1 queue_bytes=4736' not in text: raise SystemExit('telemetry queue did not use PSRAM path')\nstatus=[x for x in text.splitlines() if x.startswith('status ')]\nif len(status)<3: raise SystemExit(f'need >=3 status samples, got {len(status)}')\ndef kv(line): return dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line))\nrows=[kv(x) for x in status]\nfor r in rows:\n    if r.get('firmware_sha')!='4a5121abf2a93ba76bfc219575d2b52b8025fb03': raise SystemExit('unexpected firmware sha')\n    if r.get('outputs')!='fake-locked' or r.get('rf_ready')!='0': raise SystemExit('unsafe output mode')\n    if int(r.get('storage_queue_drops','-1')) != 0: raise SystemExit('storage queue drops detected')\n    if int(r.get('storage_write_errors','-1')) != 0: raise SystemExit('storage write errors detected')\nfor bad in ['heap_integrity_failed','arbiter_counter_regression','Guru Meditation Error','CORRUPT HEAP','Stack canary watchpoint triggered']:\n    if bad in text: raise SystemExit('unexpected fault '+bad)\nif 'stage28e_coredump present=1 valid=1' in text: raise SystemExit('unexpected valid coredump')\nheartbeats=text.count('heartbeat seq='); heap_ok=text.count('heap_integrity_ok check=')\nif heartbeats<9: raise SystemExit(f'insufficient heartbeats {heartbeats}')\nif heap_ok<1: raise SystemExit(f'insufficient heap checks {heap_ok}')\nmetrics=[]\nfor r in rows:\n    m={k:int(r[k]) for k in ['uptime_ms','internal_free','internal_min','internal_largest','psram_free','psram_min','psram_largest','stack_high_water','storage_records_written','storage_queue_drops','storage_write_errors'] if k in r}; metrics.append(m)\nif any(m.get('internal_free',0)<217000 for m in metrics): raise SystemExit('internal free did not show expected queue relocation gain')\nif any(m.get('internal_min',0)<216000 for m in metrics): raise SystemExit('internal min did not show expected queue relocation gain')\nif any(m.get('internal_largest',0)<172032 for m in metrics): raise SystemExit('largest internal block regressed')\nif any(m.get('stack_high_water',0)<10000 for m in metrics): raise SystemExit('main stack HWM regressed')\nif metrics[-1].get('storage_records_written',0) < 5: raise SystemExit('insufficient storage write activity')\nvals=[]; outputs=[]\nfor _ in range(5):\n    with urllib.request.urlopen('http://192.168.0.16/rpc/Switch.GetStatus?id=0',timeout=5) as resp: d=json.load(resp)\n    vals.append(float(d.get('apower',0.0))); outputs.append(bool(d.get('output',False))); time.sleep(0.4)\nmed=statistics.median(vals)\nprint('STAGE28E_E1_HW_METRICS',metrics)\nprint('STAGE28E_E1_HW_HEARTBEATS',heartbeats,'HEAP_OK',heap_ok)\nprint('STAGE28E_E1_HW_INTERNAL_GAIN_VS_D2_FREE',metrics[-1].get('internal_free',0)-213900)\nprint('STAGE28E_E1_HW_INTERNAL_GAIN_VS_D2_MIN',metrics[-1].get('internal_min',0)-213264)\nprint('STAGE28E_E1_HW_SHELLY output_all_on=%d median_apower_w=%.2f samples=%s'%(all(outputs),med,vals))\nif not all(outputs) or not (45.0<=med<=80.0): raise SystemExit('Shelly safety check failed')\nprint('STAGE28E_E1_QUEUE_PSRAM_HARDWARE_PASS')\nPY",
    "set -euo pipefail; test -z \"$(git status --porcelain)\"; git diff --check; echo STAGE28E_E1_QUEUE_PSRAM_SAFE_FAKE_LOCKED_PASS"
  ]
}
