{
  "version": 1,
  "id": "20260911-quality-freeze-final-physical-h-v1",
  "repository_id": "growbox-ml-controller",
  "repository": "MichalMatu/growbox-ml-controller",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "work_branch": "refactor/quality-freeze-final",
  "resources": [],
  "allow_write": false,
  "command_timeout": 7200,
  "idle_timeout": 3600,
  "task_timeout": 21600,
  "memory_limit_mb": 4096,
  "commands": [
    "git fetch -q origin refactor/quality-freeze-final && git reset --hard FETCH_HEAD && test \"$(git rev-parse HEAD)\" = \"fa56b345b5e555a68fc5a880779dee6f931acf66\" && test -z \"$(git status --porcelain)\" && test -e /dev/cu.usbserial-1130 && test ! -e /dev/cu.usbserial-10 && echo QF8_BASE_PREFLIGHT_PASS sha=$(git rev-parse HEAD) port=/dev/cu.usbserial-1130 forbidden_port_absent=1 hardware_started=0",
    "rm -rf build/qf8-final-real && PORT=/dev/cu.usbserial-1130 STAGE27C_BUILD_DIR=build/qf8-final-real GROWBOX_FIRMWARE_GIT_SHA=fa56b345b5e555a68fc5a880779dee6f931acf66 GROWBOX_RF433_LOOPBACK_ENABLED=1 GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=1 GROWBOX_RF433_REMOTE_CAPTURE_ENABLED=0 GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0 GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0 bash scripts/stage27c_crowpanel.sh flash && test -e /dev/cu.usbserial-1130 && test ! -e /dev/cu.usbserial-10 && grep '^CONFIG_ESP_MAIN_TASK_STACK_SIZE=' build/qf8-final-real/sdkconfig && echo QF8_FLASH_PASS sha=fa56b345b5e555a68fc5a880779dee6f931acf66 port=/dev/cu.usbserial-1130 real_outputs=1 rf_enabled=1 forbidden_port_untouched=/dev/cu.usbserial-10 hardware_started=1",
    "git fetch -q origin agent-control && git show FETCH_HEAD:.agent/scripts/20260910-output-supervisor-physical-h-v1.py > /tmp/growbox-qf8-final-physical-h.py && python3 - <<'PY'\nfrom pathlib import Path\np = Path('/tmp/growbox-qf8-final-physical-h.py')\ns = p.read_text()\ns = s.replace('QUALIFIED_SHA = \"02208d23f403bca3540dbbd652eb55703a044833\"', 'QUALIFIED_SHA = \"fa56b345b5e555a68fc5a880779dee6f931acf66\"', 1)\nold = '''def shelly_get(path: str) -> dict[str, object]:\n    url = f\"http://{SHELLY_HOST}{path}\"\n    with urllib.request.urlopen(url, timeout=2.0) as response:\n        payload = json.load(response)\n    if not isinstance(payload, dict):\n        raise HFailure(f\"Shelly returned non-object for {path}\")\n    return payload\n\n\ndef shelly_sample() -> tuple[bool, float]:\n    meter = shelly_get(\"/meter/0\")\n    relay = shelly_get(\"/relay/0\")\n    if \"power\" not in meter or \"ison\" not in relay:\n        raise HFailure(\"Shelly Gen1 meter/relay fields are missing\")\n    return bool(relay[\"ison\"]), float(meter[\"power\"])\n'''\nnew = '''def shelly_sample() -> tuple[bool, float]:\n    url = f\"http://{SHELLY_HOST}/rpc/Switch.GetStatus?id=0\"\n    with urllib.request.urlopen(url, timeout=3.0) as response:\n        payload = json.load(response)\n    if not isinstance(payload, dict):\n        raise HFailure(\"Shelly RPC returned non-object status\")\n    if \"output\" not in payload or \"apower\" not in payload:\n        raise HFailure(\"Shelly RPC output/apower fields are missing\")\n    return bool(payload[\"output\"]), float(payload[\"apower\"])\n'''\nif s.count(old) != 1:\n    raise SystemExit('Shelly adapter patch anchor mismatch')\ns = s.replace(old, new, 1)\nold = '''            self.identity_ok = True\n            self.output_mode_ok = outputs == \"real-bounded\"\n            self.rf_ready = rf_ready == \"1\"\n            if not self.output_mode_ok or not self.rf_ready:\n                raise HFailure(f\"real output transport not ready outputs={outputs!r} rf_ready={rf_ready!r}\")\n            return\n'''\nnew = '''            self.identity_ok = True\n            self.output_mode_ok = outputs == \"real-bounded\"\n            self.rf_ready = rf_ready == \"1\"\n            return\n'''\nif s.count(old) != 1:\n    raise SystemExit('status readiness patch anchor mismatch')\ns = s.replace(old, new, 1)\nold = '''        if observer.identity_ok and observer.sd_mounted and env_ok and safety_clear:\n'''\nnew = '''        if (\n            observer.identity_ok\n            and observer.output_mode_ok\n            and observer.rf_ready\n            and observer.sd_mounted\n            and env_ok\n            and safety_clear\n        ):\n'''\nif s.count(old) != 1:\n    raise SystemExit('wait readiness patch anchor mismatch')\ns = s.replace(old, new, 1)\nold = '''        if not observer.identity_ok:\n            raise HFailure(\"could not verify exact firmware identity\")\n        session = discover_session(observer)\n        wait_until_ready(observer)\n        run_h(observer, session)\n'''\nnew = '''        if not observer.identity_ok:\n            raise HFailure(\"could not verify exact firmware identity\")\n        wait_until_ready(observer)\n        session = discover_session(observer)\n        run_h(observer, session)\n'''\nif s.count(old) != 1:\n    raise SystemExit('startup ordering patch anchor mismatch')\ns = s.replace(old, new, 1)\nfor forbidden in ('observer.send(\"fan ', 'observer.send(\"rf ', 'observer.send(\"maintenance '):\n    if forbidden in s:\n        raise SystemExit(f'forbidden direct actuator command in H runner: {forbidden}')\nif 'QUALIFIED_SHA = \"fa56b345b5e555a68fc5a880779dee6f931acf66\"' not in s:\n    raise SystemExit('final candidate SHA patch failed')\np.write_text(s)\nPY\nPYTHONPATH=\"$PWD\" .venv/bin/python -m py_compile /tmp/growbox-qf8-final-physical-h.py && test -e /dev/cu.usbserial-1130 && test ! -e /dev/cu.usbserial-10 && echo QF8_H_RUNNER_PREFLIGHT_PASS sha=fa56b345b5e555a68fc5a880779dee6f931acf66 direct_actuator_commands=0 port=/dev/cu.usbserial-1130 forbidden_port_absent=1 hardware_started=1",
    "test -e /dev/cu.usbserial-1130 && test ! -e /dev/cu.usbserial-10 && PYTHONPATH=\"$PWD\" .venv/bin/python /tmp/growbox-qf8-final-physical-h.py && test \"$(git rev-parse HEAD)\" = \"fa56b345b5e555a68fc5a880779dee6f931acf66\" && test -z \"$(git status --porcelain)\" && echo QF8_FINAL_PHYSICAL_H_PASS sha=fa56b345b5e555a68fc5a880779dee6f931acf66 port=/dev/cu.usbserial-1130 raw_rf=0 forbidden_port_untouched=/dev/cu.usbserial-10 hardware_started=1"
  ]
}