{
  "id": "20260907-growbox-stage28e-phase-g-short-runtime-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": ["board:growbox-s3"],
  "memory_limit_mb": 6144,
  "command_timeout": 7200,
  "idle_timeout": 900,
  "task_timeout": 10800,
  "commands": [
    "set -euo pipefail; EXPECTED=389453882f0e0d2209c5bdece7eaf443895aa7ba; git fetch -q origin mvp/environment-controller; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; test -z \"$(git status --porcelain)\"; test -e /dev/cu.usbserial-1130; git diff --check; echo STAGE28E_G_SHORT_EXACT_SHA_PASS=$EXPECTED",
    "set -euo pipefail; export CMAKE_BUILD_PARALLEL_LEVEL=1; export STAGE27C_BUILD_DIR=build/idf-stage28e-g-short; export GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=0; export GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0; export GROWBOX_RF433_LOOPBACK_ENABLED=0; export GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0; export GROWBOX_FIRMWARE_GIT_SHA=389453882f0e0d2209c5bdece7eaf443895aa7ba; bash scripts/stage27c_crowpanel.sh build >/tmp/stage28e-g-short-build.log 2>&1; grep -Fq 'CONFIG_ESP_MAIN_TASK_STACK_SIZE=12288' build/idf-stage28e-g-short/sdkconfig; PORT=/dev/cu.usbserial-1130 bash scripts/stage27c_crowpanel.sh flash >/tmp/stage28e-g-short-flash.log 2>&1; echo STAGE28E_G_SHORT_FLASH_PASS",
    ".venv/bin/python - <<'PY'\nimport json,re,statistics,time,urllib.request\nimport serial\nEXPECTED='389453882f0e0d2209c5bdece7eaf443895aa7ba'\np='/dev/cu.usbserial-1130'\nh=serial.Serial(p,115200,timeout=0.1,write_timeout=1); h.dtr=False; h.rts=False\nstart=time.monotonic(); end=start+185; raw=bytearray(); sent=set()\nschedule=[(20,b'status\\n','s20'),(55,b'status\\n','s55'),(90,b'status\\n','s90'),(125,b'status\\n','s125'),(160,b'status\\n','s160'),(180,b'status\\n','s180')]\nwhile time.monotonic()<end:\n    elapsed=time.monotonic()-start\n    for at,cmd,name in schedule:\n        if elapsed>=at and name not in sent:\n            h.write(cmd); h.flush(); sent.add(name)\n    n=h.in_waiting\n    if n: raw.extend(h.read(n))\n    time.sleep(0.05)\ntime.sleep(1); n=h.in_waiting\nif n: raw.extend(h.read(n))\nh.close(); text=raw.decode(errors='replace')\nprint('STAGE28E_G_SHORT_SERIAL_CHARS',len(text)); print(text[-70000:])\nif 'queue_psram=1 queue_bytes=4736' not in text: raise SystemExit('telemetry queue PSRAM path missing')\nlife=[]\nfor line in text.splitlines():\n    if 'stage28e_lifecycle: arbiter_construct ' not in line: continue\n    d=dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line)); life.append(d)\nif len(life)!=1: raise SystemExit(f'expected exactly one arbiter construction in run, got {len(life)}')\nif int(life[0].get('construction_count','0'))!=1 or int(life[0].get('instance_id','0'))<1: raise SystemExit('unexpected arbiter lifecycle identity')\nstatus=[]\nfor line in text.splitlines():\n    if not line.startswith('status firmware_sha='): continue\n    d=dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line))\n    if d.get('firmware_sha')!=EXPECTED: raise SystemExit('unexpected firmware sha')\n    if d.get('outputs')!='fake-locked' or d.get('rf_ready')!='0': raise SystemExit('unsafe output mode')\n    status.append(d)\nif len(status)<5: raise SystemExit(f'need >=5 status samples, got {len(status)}')\nboot_ids={d.get('boot_id') for d in status}; reset_reasons={d.get('reset_reason') for d in status}\nif len(boot_ids)!=1 or None in boot_ids or '00000000' in boot_ids: raise SystemExit(f'boot identity changed/invalid {boot_ids}')\nif len(reset_reasons)!=1: raise SystemExit(f'reset reason changed {reset_reasons}')\nupt=[int(d['uptime_ms']) for d in status]\nif any(b<=a for a,b in zip(upt,upt[1:])): raise SystemExit('uptime did not increase monotonically')\nfor d in status:\n    if int(d['internal_free'])<218000: raise SystemExit('internal free below short-run threshold')\n    if int(d['internal_min'])<215000: raise SystemExit('internal minimum below short-run threshold')\n    if int(d['internal_largest'])<163840: raise SystemExit('largest internal block below threshold')\n    if int(d['psram_free'])<8300000 or int(d['psram_largest'])<8000000: raise SystemExit('PSRAM headroom regressed')\nmain=[]; store=[]; timing=[]\nfor line in text.splitlines():\n    if line.startswith('task name=main '):\n        main.append(dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line)))\n    elif line.startswith('task name=stage27_store '):\n        store.append(dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line)))\n    elif line.startswith('timing loop_samples='):\n        timing.append(dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line)))\nif len(main)<5 or len(store)<5 or len(timing)<5: raise SystemExit('insufficient task/timing samples')\nfor d in main:\n    if int(d.get('configured_stack_bytes','0'))!=12288 or int(d.get('hwm_bytes','0'))<4096: raise SystemExit('main stack margin failure')\nfor d in store:\n    if int(d.get('configured_stack_bytes','0'))!=6144 or int(d.get('hwm_bytes','0'))<1536: raise SystemExit('storage stack margin failure')\nfor d in timing:\n    if int(d.get('loop_overruns','-1'))!=0 or int(d.get('loop_max_us','1000001'))>=1000000: raise SystemExit('loop timing budget failure')\nsoak=[]\nfor line in text.splitlines():\n    if f'soak_v=2 firmware_sha={EXPECTED}' not in line: continue\n    d=dict(re.findall(r'([A-Za-z0-9_]+)=([^ ]+)',line))\n    if 'storage_queue_drops' not in d: continue\n    if d.get('outputs')!='fake-locked': raise SystemExit('unsafe soak output mode')\n    if int(d.get('storage_queue_drops','-1'))!=0 or int(d.get('storage_write_errors','-1'))!=0: raise SystemExit('telemetry storage error')\n    if int(d.get('storage_fallbacks','-1'))!=0: raise SystemExit('unexpected storage fallback')\n    if d.get('ble_scanning')!='1' or d.get('scd_sample')!='1': raise SystemExit('sensor/BLE runtime unhealthy')\n    soak.append(d)\nif len(soak)<14: raise SystemExit(f'need >=14 telemetry samples, got {len(soak)}')\nif int(soak[-1].get('storage_records_written','0'))<15: raise SystemExit('insufficient telemetry writes')\nheartbeats=text.count('heartbeat seq='); heap_ok=text.count('heap_integrity_ok check=')\nif heartbeats<14: raise SystemExit(f'insufficient heartbeats {heartbeats}')\nif heap_ok<2: raise SystemExit(f'insufficient heap integrity checks {heap_ok}')\nfor bad in ['heap_integrity_failed','arbiter_counter_regression','Guru Meditation Error','CORRUPT HEAP','Stack canary watchpoint triggered','Task watchdog got triggered','stage28e_coredump present=1 valid=1']:\n    if bad in text: raise SystemExit('unexpected fault '+bad)\nif text.count('stage28e_runtime_lifecycle entry_count=1')>1: raise SystemExit('runtime lifecycle unexpectedly re-entered')\nvals=[]; outputs=[]\nfor _ in range(5):\n    with urllib.request.urlopen('http://192.168.0.16/rpc/Switch.GetStatus?id=0',timeout=5) as resp: d=json.load(resp)\n    vals.append(float(d.get('apower',0.0))); outputs.append(bool(d.get('output',False))); time.sleep(0.4)\nif not all(outputs): raise SystemExit('Shelly master is not ON')\nlast=status[-1]; main_worst=min(int(x['hwm_bytes']) for x in main); store_worst=min(int(x['hwm_bytes']) for x in store); med=statistics.median(vals)\nprint('STAGE28E_G_SHORT_IDENTITY boot_id=%s reset_reason=%s arbiter_instance=%s constructions=%s'%(next(iter(boot_ids)),next(iter(reset_reasons)),life[0]['instance_id'],life[0]['construction_count']))\nprint('STAGE28E_G_SHORT_MEMORY free=%s min=%s largest=%s psram_free=%s psram_largest=%s'%(last['internal_free'],last['internal_min'],last['internal_largest'],last['psram_free'],last['psram_largest']))\nprint('STAGE28E_G_SHORT_STACK main_worst_hwm=%d store_worst_hwm=%d'%(main_worst,store_worst))\nprint('STAGE28E_G_SHORT_RUNTIME heartbeats=%d heap_ok=%d telemetry_samples=%d records_written=%s loop_max_us=%s'%(heartbeats,heap_ok,len(soak),soak[-1]['storage_records_written'],timing[-1]['loop_max_us']))\nprint('STAGE28E_G_SHORT_SHELLY output_all_on=1 median_apower_w=%.2f samples=%s'%(med,vals))\nprint('STAGE28E_G_SHORT_RUNTIME_PASS')\nprint('STAGE28E_G_SHORT_SAFE_FAKE_LOCKED_PASS')\nPY",
    "set -euo pipefail; test -z \"$(git status --porcelain)\"; git diff --check; echo STAGE28E_G_SHORT_HARDWARE_GATE_PASS"
  ]
}
