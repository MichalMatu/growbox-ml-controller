{
  "version": 1,
  "id": "20260910-output-supervisor-physical-h-v2",
  "repository_id": "growbox-ml-controller",
  "repository": "MichalMatu/growbox-ml-controller",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "work_branch": "mvp/environment-controller",
  "resources": [],
  "allow_write": false,
  "command_timeout": 7200,
  "idle_timeout": 3600,
  "task_timeout": 10800,
  "memory_limit_mb": 4096,
  "commands": [
    "git fetch -q origin mvp/environment-controller && git reset --hard FETCH_HEAD && test \"$(git rev-parse HEAD)\" = \"2a19cd43646fe284a7ab41828178b2b8f17edea1\" && test -z \"$(git status --porcelain)\"",
    "git fetch -q origin agent-control && git show FETCH_HEAD:.agent/scripts/20260910-output-supervisor-physical-h-v1.py > /tmp/growbox-output-supervisor-physical-h-v2.py && python3 - <<'PY'\nfrom pathlib import Path\np = Path('/tmp/growbox-output-supervisor-physical-h-v2.py')\ns = p.read_text()\nold = '''            self.identity_ok = True\n            self.output_mode_ok = outputs == \"real-bounded\"\n            self.rf_ready = rf_ready == \"1\"\n            if not self.output_mode_ok or not self.rf_ready:\n                raise HFailure(f\"real output transport not ready outputs={outputs!r} rf_ready={rf_ready!r}\")\n            return\n'''\nnew = '''            self.identity_ok = True\n            self.output_mode_ok = outputs == \"real-bounded\"\n            self.rf_ready = rf_ready == \"1\"\n            return\n'''\nif s.count(old) != 1:\n    raise SystemExit('status readiness patch anchor mismatch')\ns = s.replace(old, new, 1)\nold = '''        if observer.identity_ok and observer.sd_mounted and env_ok and safety_clear:\n'''\nnew = '''        if (\n            observer.identity_ok\n            and observer.output_mode_ok\n            and observer.rf_ready\n            and observer.sd_mounted\n            and env_ok\n            and safety_clear\n        ):\n'''\nif s.count(old) != 1:\n    raise SystemExit('wait readiness patch anchor mismatch')\ns = s.replace(old, new, 1)\nold = '''        if not observer.identity_ok:\n            raise HFailure(\"could not verify exact firmware identity\")\n        session = discover_session(observer)\n        wait_until_ready(observer)\n        run_h(observer, session)\n'''\nnew = '''        if not observer.identity_ok:\n            raise HFailure(\"could not verify exact firmware identity\")\n        wait_until_ready(observer)\n        session = discover_session(observer)\n        run_h(observer, session)\n'''\nif s.count(old) != 1:\n    raise SystemExit('startup ordering patch anchor mismatch')\ns = s.replace(old, new, 1)\nfor forbidden in ('observer.send(\"fan ', 'observer.send(\"rf ', 'observer.send(\"maintenance '):\n    if forbidden in s:\n        raise SystemExit(f'forbidden direct actuator command in H runner: {forbidden}')\np.write_text(s)\nPY\nPYTHONPATH=\"$PWD\" .venv/bin/python -m py_compile /tmp/growbox-output-supervisor-physical-h-v2.py && echo H_RUNNER_V2_PREFLIGHT_PASS tooling_sha=2a19cd43646fe284a7ab41828178b2b8f17edea1 qualified_sha=02208d23f403bca3540dbbd652eb55703a044833 startup_wait_s=900 direct_actuator_commands=0 serial_started=0 hardware_started=0",
    "test -e /dev/cu.usbserial-1130 && test ! -e /dev/cu.usbserial-10 && PYTHONPATH=\"$PWD\" .venv/bin/python /tmp/growbox-output-supervisor-physical-h-v2.py"
  ]
}
