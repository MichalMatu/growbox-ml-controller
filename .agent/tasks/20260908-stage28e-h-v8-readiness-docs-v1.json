{
  "id": "20260908-stage28e-h-v8-readiness-docs-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": true,
  "resources": [],
  "memory_limit_mb": 4096,
  "command_timeout": 600,
  "idle_timeout": 180,
  "task_timeout": 1200,
  "commands": [
    "set -euo pipefail; EXPECTED_HEAD=231eed28f64bdbdc4238fd8bce128264027702f2; git fetch -q origin mvp/environment-controller:refs/remotes/origin/mvp/environment-controller; test \"$(git rev-parse HEAD)\" = \"$EXPECTED_HEAD\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED_HEAD\"; test -z \"$(git status --porcelain)\"; python3 - <<'PY'\nfrom pathlib import Path\nimport re\n\nqualified='5a4830db9d10e8cb73d4c617b09122f0844ad899'\nobserver='45065a34ce276ac5cdb7ef8cf0a1ad8a4bae1b0d'\npreflight_head='231eed28f64bdbdc4238fd8bce128264027702f2'\npreflight_result='.agent/results/20260908-stage28e-h-v8-preflight-v1.json'\n\ndef replace_section(text, start_heading, end_heading, body):\n    pattern=re.escape(start_heading)+r'.*?(?='+re.escape(end_heading)+r')'\n    new=start_heading+'\\n\\n'+body.rstrip()+'\\n\\n'\n    updated,count=re.subn(pattern,new,text,flags=re.S)\n    assert count==1,(start_heading,count)\n    return updated\n\n# CONTINUATION_PLAN\np=Path('docs/CONTINUATION_PLAN.md'); s=p.read_text(); s=s.replace('Updated: 2026-09-07','Updated: 2026-09-08',1)\nlatest=(\n'Latest physical attempt: **H v7**.\\n\\n'\n'- task: `.agent/tasks/20260907-stage28e-h-v7-corrected-parser-v1.json`;\\n'\n'- result: `.agent/results/20260907-stage28e-h-v7-corrected-parser-v1.json`;\\n'\n'- primary was intentionally interrupted with RC `130` after the qualification harness defect was identified;\\n'\n'- mandatory recovery and final RF-disabled `fake-locked` both passed (`recovery=0`, `final=0`);\\n'\n'- retained v7 telemetry showed normal fan cycling with `safety_latched=0`, `force_fan=0`, and `safety_reason=1` (`TimerOff`), so the old `reason == 0` observer gate was a false-negative condition rather than production safety activation.\\n\\n'\n'The observer fix is commit `'+observer+'`. It accepts only `Safe (0)` and `TimerOff (1)` as H safety-clear reasons, and only while both `safety_latched=0` and `force_fan=0`; reasons `2..5` remain rejected. Replay of the retained v7 log accepted `596` TimerOff samples and no unsafe reason.\\n\\n'\n'H v8 software preflight `20260908-stage28e-h-v8-preflight-v1` is **PASS** on executable/preflight HEAD `'+preflight_head+'`. It proved no production C/C++ delta relative to `'+qualified+'`, passed the focused observer tests and v7 replay, passed all `24/24` host tests, built fake `12288 B` main-stack firmware, built real/recovery `16384 B` main-stack firmware, and ended clean with `hardware_started=0`. H v8 itself has **not** been started.'\n)\ns=replace_section(s,'## Latest H evidence','## Repository-tracked closed-tent observer',latest)\ns=s.replace('## Software-only gate before corrected H v7','## Software-only gate before H v8')\ngate=(\n'H v8 preflight `20260908-stage28e-h-v8-preflight-v1` is **PASS** on `'+preflight_head+'`. It verified the TimerOff-aware observer, v7 replay (`596` accepted TimerOff samples), all `24/24` host tests, fake `12288 B` build, real/recovery `16384 B` builds, no production C/C++ delta from `'+qualified+'`, and a clean worktree.\\n\\n'\n'Committed documentation-only readiness changes after that preflight are allowed without rebuilding. Before H v8 starts, verify the current worktree is clean and prove any delta after `'+preflight_head+'` is documentation-only; any executable/configuration change requires a new software preflight.'\n)\ns=replace_section(s,'## Software-only gate before H v8','## Overnight bounded-H wrapper',gate)\nnew_boot=(\n'`Continue Growbox Stage28E Phase H only in MichalMatu/growbox-ml-controller. Read AGENTS.md, docs/STAGE28E_PHASE_H_HANDOFF.md, docs/CURRENT_STATUS.md, docs/CONTINUATION_PLAN.md, docs/GUIDANCE.md and docs/ESP32_S3_SERIAL_PORT_RESET.md, then fresh-check mvp/environment-controller HEAD and agent-control:.agent/status/daemon.json. A-G are complete; H is open. Qualified production firmware identity remains '+qualified+'. TimerOff-aware observer commit is '+observer+'. H v8 software preflight 20260908-stage28e-h-v8-preflight-v1 passed on '+preflight_head+' with 24/24 host tests, v7 replay accepted 596 TimerOff samples, fake 12 KiB and real/recovery 16 KiB images built, no production C/C++ delta, and hardware_started=0. H v8 is prepared but not started. All repository tasks use resources: []; verify /dev/cu.usbserial-1130 inside any hardware task and never touch /dev/cu.usbserial-10. When explicitly authorized to start H v8, use only the normal controller path, no request injection or actuator forcing, and always perform recovery plus final RF-disabled fake-locked. Do not begin the production runtime/service-console refactor before formal H PASS.`'\n)\ns=re.sub(r'## Recommended fresh-chat instruction\\n.*\\Z','## Recommended fresh-chat instruction\\n\\n'+new_boot+'\\n',s,flags=re.S)\np.write_text(s)\n\n# CURRENT_STATUS\np=Path('docs/CURRENT_STATUS.md'); s=p.read_text(); s=s.replace('Updated: 2026-09-07','Updated: 2026-09-08',1)\ninsert=(\n'## H v7 TimerOff false negative and H v8 readiness\\n\\n'\n'H v7 primary was intentionally interrupted with RC `130` once the observer semantic defect was established. Recovery and final verification both passed, so the board returned to RF-disabled `fake-locked`. V7 telemetry showed normal fan transitions while `safety_latched=0`, `force_fan=0`, `safety_reason=1`; production defines reason `1` as `TimerOff`, a normal schedule-off state.\\n\\n'\n'Observer commit `'+observer+'` now treats only `Safe (0)` and `TimerOff (1)` as H safety-clear when both latch and force are zero; reasons `2..5` remain rejected. Focused tests pass and retained v7 replay accepts `596` TimerOff samples with zero unsafe accepts.\\n\\n'\n'H v8 software preflight `20260908-stage28e-h-v8-preflight-v1` passed on `'+preflight_head+'`: no production C/C++ delta from `'+qualified+'`, `24/24` host tests passed, fake `12288 B` and real/recovery `16384 B` images were built, final tree was clean, and no hardware was started. **H v8 itself is not running and has not been started.**\\n\\n'\n)\nanchor='## Closed-tent H release-candidate harness\\n'\nassert anchor in s and '## H v7 TimerOff false negative and H v8 readiness' not in s\ns=s.replace(anchor,insert+anchor,1)\ns=s.replace('- use `resources: []` for software/docs/build work;\\n- use `resources: []` for serial/flash/hardware;','- use `resources: []` for every repository task, including software, builds, serial, flash and hardware;\\n- verify the exact device/port inside hardware tasks;')\nnextwork=(\n'1. Treat `'+preflight_head+'` as the H v8 executable/preflight identity and allow only documentation-only readiness commits after it without rerunning the software gate.\\n'\n'2. H v8 is prepared but **must not start until explicitly requested**. When started, its task must use `resources: []`, verify `/dev/cu.usbserial-1130` inside the task and explicitly refuse `/dev/cu.usbserial-10`.\\n'\n'3. Start with the tent already closed; require no operator action, no request injection and no actuator forcing.\\n'\n'4. Prove stable safety-clear physical fan OFF -> natural `requested_fan>=0.10` -> normal arbiter OFF->ON -> RF TX increment with zero errors -> unconfounded Shelly physical support. `Safe (0)` and `TimerOff (1)` are acceptable only while `safety_latched=0` and `force_fan=0`; reasons `2..5` are not.\\n'\n'5. Regardless of primary outcome, always execute recovery RF and final RF-disabled `fake-locked`.\\n'\n'6. If H v8 passes, formally close Stage28E H before starting any longer soak or production runtime/service-console modularization.\\n'\n'7. Keep the production runtime/service-console modularity refactor deferred until H is formally closed.'\n)\ns=re.sub(r'## Immediate next work\\n.*\\Z','## Immediate next work\\n\\n'+nextwork+'\\n',s,flags=re.S)\np.write_text(s)\n\n# PHASE_H_HANDOFF\np=Path('docs/STAGE28E_PHASE_H_HANDOFF.md'); s=p.read_text(); s=s.replace('Updated: 2026-09-07','Updated: 2026-09-08',1)\nreadiness=(\n'## H v7 TimerOff observer false negative and H v8 readiness\\n\\n'\n'H v7 (`20260907-stage28e-h-v7-corrected-parser-v1`) was intentionally interrupted after the observer semantic defect was identified. Primary RC was `130`; mandatory recovery and final verification were both `0`, leaving the board RF-disabled and `fake-locked`.\\n\\n'\n'The retained run showed `safety_latched=0`, `force_fan=0`, `safety_reason=1` while the normal fan path repeatedly transitioned. Production `LampSafetyReason` defines `1` as `TimerOff`, so requiring `safety_reason==0` prevented a valid nighttime baseline. This was a harness/evidence defect, not production C++ behavior.\\n\\n'\n'Observer commit `'+observer+'` accepts only reasons `0` (`Safe`) and `1` (`TimerOff`) when both `safety_latched=0` and `force_fan=0`; reasons `2..5` remain rejected. Focused regression tests pass, and retained v7 replay accepted `596` TimerOff samples with zero unsafe accepts.\\n\\n'\n'H v8 software preflight `20260908-stage28e-h-v8-preflight-v1` is **PASS** on `'+preflight_head+'`. It verified no production C/C++ delta relative to `'+qualified+'`, the TimerOff-aware observer, `24/24` host tests, fake `12288 B` main-stack image, real/recovery `16384 B` main-stack images, and a clean tree. Terminal marker records `resources=empty hardware_started=0`. H v8 has **not** been started.\\n\\n'\n)\nanchor='## Closed-tent observer — release candidate\\n'\nassert anchor in s and '## H v7 TimerOff observer false negative and H v8 readiness' not in s\ns=s.replace(anchor,readiness+anchor,1)\ns=s.replace('Hardware resource:\\n\\n`\"resources\": [\"board:growbox-s3\"]`','Task resources:\\n\\n`\"resources\": []`')\ns=s.replace('For the immediate corrected H v7 qualification,','For the prepared H v8 qualification,')\ns=s.replace('- `resources: []` for software-only work;\\n- `resources: []` for serial/flash/device work;','- `resources: []` for every repository task, including software/build/serial/flash/device work;\\n- hardware tasks verify the exact device/port internally;')\nboot=(\n'> Continue Growbox Stage28E Phase H only in `MichalMatu/growbox-ml-controller`. First read `AGENTS.md`, `docs/STAGE28E_PHASE_H_HANDOFF.md`, `docs/CURRENT_STATUS.md`, `docs/CONTINUATION_PLAN.md`, `docs/GUIDANCE.md`, and `docs/ESP32_S3_SERIAL_PORT_RESET.md`. Fresh-check `mvp/environment-controller` HEAD and `agent-control:.agent/status/daemon.json`. A-G are complete; H remains open. Qualified production firmware identity is `'+qualified+'`; TimerOff-aware observer commit is `'+observer+'`; H v8 software preflight `20260908-stage28e-h-v8-preflight-v1` passed on `'+preflight_head+'` with `24/24` host tests, v7 replay acceptance of `596` TimerOff samples, fake 12 KiB and real/recovery 16 KiB images, no production C/C++ delta and `hardware_started=0`. H v8 is prepared but not started. All repository tasks use `resources: []`; any hardware task must verify `/dev/cu.usbserial-1130` internally and refuse `/dev/cu.usbserial-10`. When H v8 is explicitly started, prove only the normal fan path and always finish with recovery plus final RF-disabled `fake-locked`. Do not add request/output forcing or begin production modularization before formal H PASS.'\n)\ns=re.sub(r'> Continue Growbox Stage28E Phase H only.*\\Z',boot+'\\n',s,flags=re.S)\np.write_text(s)\nprint('H_V8_READINESS_DOCS_PATCHED')\nPY\ngit diff --check; python3 - <<'PY'\nimport subprocess\npaths=set(subprocess.check_output(['git','diff','--name-only'],text=True).splitlines())\nexpected={'docs/CONTINUATION_PLAN.md','docs/CURRENT_STATUS.md','docs/STAGE28E_PHASE_H_HANDOFF.md'}\nassert paths==expected,(paths,expected)\nassert not any(p.endswith(('.c','.cc','.cpp','.h','.hpp','.py')) for p in paths),paths\nprint('H_V8_READINESS_DOCS_SCOPE_PASS')\nPY\ngit add docs/CONTINUATION_PLAN.md docs/CURRENT_STATUS.md docs/STAGE28E_PHASE_H_HANDOFF.md; git diff --cached --check; git commit -m \"Record Phase H v8 readiness\"; git push origin HEAD:mvp/environment-controller; test -z \"$(git status --porcelain)\"; echo H_V8_READINESS_DOCS_PASS head=$(git rev-parse HEAD)"
  ]
}
