{
  "version": 1,
  "id": "20260912-deep-structure-reaudit-v1",
  "repository_id": "growbox-ml-controller",
  "repository": "MichalMatu/growbox-ml-controller",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "work_branch": "main",
  "resources": [],
  "allow_write": false,
  "command_timeout": 600,
  "idle_timeout": 180,
  "task_timeout": 1800,
  "memory_limit_mb": 3072,
  "commands": [
    "P=\"$HOME/Documents/PlatformIO/Projects/ml\"; test -d \"$P/.git\"; O=$(git -C \"$P\" remote get-url origin); case \"$O\" in https://github.com/MichalMatu/growbox-ml-controller|https://github.com/MichalMatu/growbox-ml-controller.git|git@github.com:MichalMatu/growbox-ml-controller.git|ssh://git@github.com/MichalMatu/growbox-ml-controller.git) ;; *) echo BOUND_REPO_MISMATCH; exit 42;; esac; git -C \"$P\" fetch -q origin main; test \"$(git -C \"$P\" branch --show-current)\" = main; test \"$(git -C \"$P\" rev-parse HEAD)\" = \"a07b5a14f4bb3ab8b8ca866aafb52a1390ef69ab\"; test \"$(git -C \"$P\" rev-parse origin/main)\" = \"a07b5a14f4bb3ab8b8ca866aafb52a1390ef69ab\"; test -z \"$(git -C \"$P\" status --porcelain)\"; echo AUDIT_BASE_OK=$(git -C \"$P\" rev-parse HEAD)",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && python3 - <<'PY'\nfrom pathlib import Path\nfrom collections import Counter, defaultdict\nroot=Path('src/climate')\nfiles=[p for p in root.rglob('*') if p.is_file() and p.suffix in {'.cpp','.h','.hpp','.c'}]\nprint('=== STRUCTURE SUMMARY ===')\nprint('SOURCE_FILES',len(files))\nbydir=Counter(str(p.parent) for p in files)\nfor d,n in sorted(bydir.items(), key=lambda x:(x[0].count('/'),x[0])):\n    print(f'DIR|{d}|files={n}')\nprint('=== TOP-LEVEL FILES ===')\nfor p in sorted([p for p in files if p.parent==root]):\n    print(p)\nprint('TOP_LEVEL_COUNT',sum(p.parent==root for p in files))\nprint('=== LARGEST FILES BY LINES ===')\nrows=[]\nfor p in files:\n    try: n=sum(1 for _ in p.open(errors='ignore'))\n    except OSError: continue\n    rows.append((n,str(p)))\nfor n,p in sorted(rows, reverse=True)[:40]: print(f'{n:5d} {p}')\nprint('=== DUPLICATE BASENAMES ===')\nbase=defaultdict(list)\nfor p in files: base[p.name].append(str(p))\nfor name,ps in sorted(base.items()):\n    if len(ps)>1: print(name,'|',' | '.join(ps))\nPY",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && python3 - <<'PY'\nfrom pathlib import Path\nfrom collections import Counter,defaultdict\nimport re\nfiles=[p for p in Path('src/climate').rglob('*') if p.is_file() and p.suffix in {'.cpp','.h','.hpp'}]\ncanon={str(p):p for p in files}\nby_suffix=defaultdict(list)\nfor s,p in canon.items():\n    rel=s.replace('src/','',1)\n    by_suffix[rel].append(s)\ninc_re=re.compile(r'^\\s*#\\s*include\\s*[\"<]([^\">]+)[\">]')\ng=defaultdict(set); incoming=Counter(); outgoing=Counter(); unresolved=Counter()\nfor s,p in canon.items():\n    for line in p.read_text(errors='ignore').splitlines():\n        m=inc_re.match(line)\n        if not m: continue\n        x=m.group(1)\n        candidates=[]\n        if x.startswith('climate/'):\n            candidates=by_suffix.get(x,[])\n        elif x.startswith('src/climate/'):\n            candidates=[x] if x in canon else []\n        if len(candidates)==1:\n            t=candidates[0]; g[s].add(t)\n        elif x.startswith('climate/'):\n            unresolved[x]+=1\nfor s,ts in g.items():\n    outgoing[s]=len(ts)\n    for t in ts: incoming[t]+=1\nprint('=== INCLUDE FAN-IN ===')\nfor p,n in incoming.most_common(30): print(f'{n:3d} {p}')\nprint('=== INCLUDE FAN-OUT ===')\nfor p,n in outgoing.most_common(30): print(f'{n:3d} {p}')\nprint('=== INTERNAL INCLUDE EDGES ===',sum(len(v) for v in g.values()))\n# Tarjan SCC\nidx=0; stack=[]; on=set(); ids={}; low={}; scc=[]\ndef dfs(v):\n    global idx\n    ids[v]=low[v]=idx; idx+=1; stack.append(v); on.add(v)\n    for w in g.get(v,()):\n        if w not in ids:\n            dfs(w); low[v]=min(low[v],low[w])\n        elif w in on: low[v]=min(low[v],ids[w])\n    if low[v]==ids[v]:\n        comp=[]\n        while True:\n            w=stack.pop(); on.remove(w); comp.append(w)\n            if w==v: break\n        scc.append(comp)\nfor v in canon:\n    if v not in ids: dfs(v)\ncy=[c for c in scc if len(c)>1]\nprint('=== INCLUDE CYCLES/SCC >1 ===',len(cy))\nfor c in sorted(cy,key=len,reverse=True): print('SCC',len(c),'|',' | '.join(sorted(c)))\nif unresolved:\n    print('=== UNRESOLVED climate/ INCLUDES ===')\n    for x,n in unresolved.most_common(): print(n,x)\nPY",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && python3 - <<'PY'\nfrom pathlib import Path\nimport re\nprod=[p for p in Path('src/climate').rglob('*.cpp')]\ntext='\\n'.join(p.read_text(errors='ignore') for p in [Path('src/CMakeLists.txt'),*Path('test').rglob('CMakeLists.txt')] if p.exists())\nprint('=== CPP WIRING CHECK ===')\nfor p in sorted(prod):\n    rel=str(p)\n    name=p.name\n    hits=text.count(rel)+text.count(rel.replace('src/','',1))+text.count(name)\n    if hits==0: print('NO_CMAKE_TEXT_REF',rel)\nprint('PROD_CPP_COUNT',len(prod))\nprint('=== CMAKE CLIMATE REFERENCES ===')\nfor i,line in enumerate(Path('src/CMakeLists.txt').read_text(errors='ignore').splitlines(),1):\n    if 'climate/' in line: print(f'{i}: {line}')\nPY",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && printf '%s\\n' '=== STAGE-NAMED ACTIVE SOURCE FILES ==='; git ls-files 'src/climate/*' | grep -E '/Stage(27|28|28d|28e)[^/]*\\.(cpp|h|hpp)$' || true; printf '%s\\n' '=== STAGE SYMBOL REFERENCES OUTSIDE COMPAT/DIAGNOSTICS/STORAGE/TELEMETRY ==='; git grep -n -E 'Stage27|Stage28|Stage28d|Stage28e' -- src/climate ':!src/climate/compatibility/**' ':!src/climate/runtime/diagnostics/**' ':!src/climate/runtime/console/**' ':!src/climate/storage/**' ':!src/climate/telemetry/**' || true; printf '%s\\n' '=== TODO FIXME HACK XXX ==='; git grep -n -E 'TODO|FIXME|HACK|XXX' -- src/climate || true",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && python3 - <<'PY'\nfrom pathlib import Path\nfrom collections import Counter\nroots=[Path('src/climate'),Path('test')]\nprod=[p for p in Path('src/climate').rglob('*') if p.is_file() and p.suffix in {'.cpp','.h'}]\ntest_text='\\n'.join(p.read_text(errors='ignore') for p in Path('test').rglob('*') if p.is_file() and p.suffix in {'.cpp','.h','.txt'})\nprint('=== PRODUCTION BASENAMES WITH NO TEST TEXT MENTION ===')\nrows=[]\nfor p in prod:\n    stem=p.stem\n    if stem not in test_text:\n        rows.append(str(p))\nfor p in rows: print(p)\nprint('NO_TEST_MENTION_COUNT',len(rows),'OF',len(prod))\nprint('=== TEST DIR COUNTS ===')\nc=Counter(str(p.parent) for p in Path('test').rglob('test_main.cpp'))\nfor d,n in sorted(c.items()): print(d,n)\nPY",
    "cd \"$HOME/Documents/PlatformIO/Projects/ml\" && printf '%s\\n' '=== DIRECTORY TREE src/climate depth<=3 ==='; find src/climate -maxdepth 3 -type d | sort; printf '%s\\n' '=== GIT STATUS ==='; git status --short --branch"
  ]
}
