{
  "id": "20260907-growbox-stage28e-phase-h-physical-e2e-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": ["board:growbox-s3"],
  "memory_limit_mb": 4096,
  "command_timeout": 2100,
  "idle_timeout": 1200,
  "task_timeout": 3300,
  "commands": [
    "set -euo pipefail; EXPECTED=1eb2772f65ddfa82ba185e5658b2c41899f118d4; git fetch -q origin mvp/environment-controller; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; test -z \"$(git status --porcelain)\"; git diff --check; echo STAGE28E_H_EXACT_SHA_PASS=$EXPECTED",
    "set -euo pipefail; export CMAKE_BUILD_PARALLEL_LEVEL=1; EXPECTED=1eb2772f65ddfa82ba185e5658b2c41899f118d4; REAL=build/stage28e-h-real-v1; REC=build/stage28e-h-recovery-rf-v1; SAFE=build/stage28e-h-final-fake-v1; rm -rf \"$REAL\" \"$REC\" \"$SAFE\"; STAGE27C_BUILD_DIR=\"$REAL\" GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=1 GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0 GROWBOX_RF433_LOOPBACK_ENABLED=1 GROWBOX_RF433_LOOPBACK_AUTO_SMOKE=0 GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0 GROWBOX_FIRMWARE_GIT_SHA=\"$EXPECTED\" bash scripts/stage27c_crowpanel.sh build; STAGE27C_BUILD_DIR=\"$REC\" GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=0 GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0 GROWBOX_RF433_LOOPBACK_ENABLED=1 GROWBOX_RF433_LOOPBACK_AUTO_SMOKE=0 GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0 GROWBOX_FIRMWARE_GIT_SHA=\"$EXPECTED\" bash scripts/stage27c_crowpanel.sh build; STAGE27C_BUILD_DIR=\"$SAFE\" GROWBOX_STAGE28_REAL_OUTPUTS_ENABLED=0 GROWBOX_STAGE28_THERMAL_TEST_SEQUENCE_ENABLED=0 GROWBOX_RF433_LOOPBACK_ENABLED=0 GROWBOX_RF433_LOOPBACK_AUTO_SMOKE=0 GROWBOX_STAGE28E_BREADCRUMB_RESTART_SELFTEST=0 GROWBOX_FIRMWARE_GIT_SHA=\"$EXPECTED\" bash scripts/stage27c_crowpanel.sh build; echo STAGE28E_H_RECOVERY_IMAGES_READY_PASS real=$REAL recovery=$REC final=$SAFE sha=$EXPECTED",
    "set -uo pipefail; EXPECTED=1eb2772f65ddfa82ba185e5658b2c41899f118d4; PORT=/dev/cu.usbserial-1130; REAL=build/stage28e-h-real-v1; REC=build/stage28e-h-recovery-rf-v1; SAFE=build/stage28e-h-final-fake-v1; test \"$PORT\" = /dev/cu.usbserial-1130 || exit 90; test -e \"$PORT\" || exit 91; . /Users/michal/esp/esp-idf/export.sh >/dev/null; run_rc=0; idf.py -B \"$REAL\" -p \"$PORT\" flash || run_rc=$?; if [ \"$run_rc\" -eq 0 ]; then EXPECTED=\"$EXPECTED\" PORT=\"$PORT\" python3 - <<'PY' || run_rc=$?
import os,re,time,json,statistics,urllib.request
import serial
EXPECTED=os.environ['EXPECTED']; PORT=os.environ['PORT']
START=time.monotonic(); DEADLINE=START+780.0
h=serial.Serial(port=None,baudrate=115200,timeout=0.1,write_timeout=1)
h.dtr=False; h.rts=False; h.port=PORT; h.open()
open_t=time.monotonic(); baseline=None; baseline_t=None; lines=[]; outputs=[]; pre_power=[]; post_power=[]
last_status_send=0.0; pre=None; transition=None; bad_after=[]; first_req=None
patkv=re.compile(r'([A-Za-z0-9_]+)=([^ ]+)')
def shelly():
    try:
        with urllib.request.urlopen('http://192.168.0.16/rpc/Switch.GetStatus?id=0',timeout=3) as r: s=json.load(r)
        return bool(s.get('output',False)),float(s.get('apower',0.0))
    except Exception:
        return None,None
while time.monotonic()<DEADLINE:
    now=time.monotonic(); raw=h.readline()
    if raw:
        line=raw.decode(errors='replace').strip(); lines.append((now,line)); print(line)
        if baseline is not None:
            if 'ESP-ROM:esp32s3-' in line or 'stage28e_runtime_lifecycle entry_count=' in line:
                bad_after.append(line)
        if line.startswith('status firmware_sha='):
            x=dict(patkv.findall(line))
            if x.get('firmware_sha')==EXPECTED and x.get('outputs')=='real-bounded' and x.get('rf_ready')=='1' and int(x.get('uptime_ms','0'))>=5000:
                if baseline is None:
                    baseline=x; baseline_t=now
                    print('STAGE28E_H_BASELINE boot_id=%s reset_reason=%s uptime_ms=%s outputs=%s rf_ready=%s'%(x.get('boot_id'),x.get('reset_reason'),x.get('uptime_ms'),x.get('outputs'),x.get('rf_ready')))
                else:
                    assert x.get('boot_id')==baseline.get('boot_id'),(baseline,x)
        if line.startswith('stage28d_output ') and baseline is not None:
            x=dict(patkv.findall(line)); outputs.append(x)
            try:
                req=float(x.get('requested_fan','0')); fan=int(x.get('fan_on','0')); applied=float(x.get('applied_fan','0')); safety=int(x.get('safety_latched','0')); force=int(x.get('force_fan','0')); reason=int(x.get('safety_reason','0')); trans=int(x.get('arbiter_transitions','0')); dwell=int(x.get('arbiter_dwell_holds','0')); tx=int(x.get('tx','0')); err=int(x.get('tx_errors','0'))
            except Exception:
                continue
            if safety==0 and force==0 and reason==0 and fan==0:
                if pre is None or req>=0.10:
                    pre=x
                if req>=0.10 and first_req is None:
                    first_req=(now,x); print('STAGE28E_H_REQUEST_SEEN requested_fan=%.3f transitions=%d dwell=%d tx=%d'%(req,trans,dwell,tx))
            if first_req is not None and safety==0 and force==0 and reason==0 and req>=0.10 and fan==1 and applied>=0.99 and err==0:
                ftrans=int(first_req[1].get('arbiter_transitions','0')); ftx=int(first_req[1].get('tx','0'))
                if trans>ftrans and tx>ftx:
                    transition=(now,x); print('STAGE28E_H_TRANSITION_SEEN requested_fan=%.3f fan_on=1 applied_fan=%.3f transitions=%d dwell=%d tx=%d tx_errors=%d'%(req,applied,trans,dwell,tx,err)); break
    if now-open_t>=6 and now-last_status_send>=20:
        try: h.write(b'status\n'); h.flush()
        except Exception: pass
        last_status_send=now
    if baseline is not None and transition is None and int(now*2)%4==0:
        on,p=shelly()
        if on is not None:
            if first_req is None: pre_power.append(p)
            elif transition is None: pre_power.append(p)
    time.sleep(0.01)
assert baseline is not None,'no stabilized real-bounded baseline'
assert not bad_after,('post-baseline reset/lifecycle marker',bad_after[-3:])
assert first_req is not None,'natural requested_fan>=0.10 not observed in bounded window'
assert transition is not None,'AH request did not complete fan transition in bounded window'
# Supporting Shelly evidence after transition.
for _ in range(8):
    on,p=shelly();
    if on is not None:
        assert on,'Shelly master output turned off'; post_power.append(p)
    time.sleep(0.5)
assert post_power,'no Shelly post-transition evidence'
px=pre or first_req[1]; tx=transition[1]
print('STAGE28E_H_E2E_PRE requested_fan=%s fan_on=%s applied_fan=%s transitions=%s dwell=%s tx=%s tx_errors=%s'%(px.get('requested_fan'),px.get('fan_on'),px.get('applied_fan'),px.get('arbiter_transitions'),px.get('arbiter_dwell_holds'),px.get('tx'),px.get('tx_errors')))
print('STAGE28E_H_E2E_TRANSITION requested_fan=%s fan_on=%s applied_fan=%s transitions=%s dwell=%s tx=%s tx_errors=%s'%(tx.get('requested_fan'),tx.get('fan_on'),tx.get('applied_fan'),tx.get('arbiter_transitions'),tx.get('arbiter_dwell_holds'),tx.get('tx'),tx.get('tx_errors')))
print('STAGE28E_H_SHELLY_SUPPORT output_on=1 pre_median=%s post_median=%.2f post_samples=%s'%('%.2f'%statistics.median(pre_power[-8:]) if pre_power else 'na',statistics.median(post_power),post_power))
print('STAGE28E_H_PHYSICAL_E2E_OBSERVATION_PASS')
h.close()
PY
fi; echo STAGE28E_H_PRIMARY_RC=$run_rc; recovery_rc=0; idf.py -B \"$REC\" -p \"$PORT\" flash || recovery_rc=$?; if [ \"$recovery_rc\" -eq 0 ]; then EXPECTED=\"$EXPECTED\" PORT=\"$PORT\" python3 - <<'PY' || recovery_rc=$?
import os,time,re,json,statistics,urllib.request,serial
P=os.environ['PORT']; E=os.environ['EXPECTED']; h=serial.Serial(port=None,baudrate=115200,timeout=0.1,write_timeout=1); h.dtr=False; h.rts=False; h.port=P; h.open(); t=time.monotonic(); buf=[]
# Allow port-open reset then establish fake-locked recovery console.
while time.monotonic()-t<12:
    s=h.readline().decode(errors='replace');
    if s: buf.append(s); print(s.strip())
    if time.monotonic()-t>6: break
for cmd in [b'rf fan off\n',b'rf humidifier off\n',b'rf lamp on\n',b'status\n']:
    h.write(cmd); h.flush(); time.sleep(1.3)
end=time.monotonic()+8
while time.monotonic()<end:
    s=h.readline().decode(errors='replace');
    if s: buf.append(s); print(s.strip())
out=''.join(buf)
assert ('outputs=fake-locked' in out or 'Automatic output mode: fake-locked.' in out), 'recovery image not fake-locked'
assert 'rf_ready=1' in out or 'transport_ready=1' in out, 'recovery RF transport unavailable'
# Require no manual-TX blocked message and require fan/humidifier OFF commands were accepted by transport output.
assert 'manual RF TX blocked while automatic outputs are real-bounded' not in out
vals=[]; ons=[]
for _ in range(5):
    with urllib.request.urlopen('http://192.168.0.16/rpc/Switch.GetStatus?id=0',timeout=3) as r: s=json.load(r)
    ons.append(bool(s.get('output',False))); vals.append(float(s.get('apower',0.0))); time.sleep(0.4)
assert all(ons),'Shelly master not ON during recovery'
print('STAGE28E_H_MANUAL_SAFE_RECOVERY_PASS outputs=fake-locked fan=off humidifier=off lamp=on shelly_master=on median_apower_w=%.2f samples=%s'%(statistics.median(vals),vals))
h.close()
PY
fi; final_rc=0; idf.py -B \"$SAFE\" -p \"$PORT\" flash || final_rc=$?; if [ \"$final_rc\" -eq 0 ]; then EXPECTED=\"$EXPECTED\" PORT=\"$PORT\" python3 - <<'PY' || final_rc=$?
import os,time,serial,re,json,statistics,urllib.request
P=os.environ['PORT']; E=os.environ['EXPECTED']; h=serial.Serial(port=None,baudrate=115200,timeout=0.1,write_timeout=1); h.dtr=False; h.rts=False; h.port=P; h.open(); start=time.monotonic(); out=[]; sent=False
while time.monotonic()-start<18:
    if time.monotonic()-start>7 and not sent: h.write(b'status\n'); h.flush(); sent=True
    s=h.readline().decode(errors='replace');
    if s: out.append(s); print(s.strip())
text=''.join(out); assert f'firmware_sha={E}' in text; assert 'outputs=fake-locked' in text; assert 'rf_ready=0' in text
vals=[]; ons=[]
for _ in range(5):
    with urllib.request.urlopen('http://192.168.0.16/rpc/Switch.GetStatus?id=0',timeout=3) as r: st=json.load(r)
    ons.append(bool(st.get('output',False))); vals.append(float(st.get('apower',0.0))); time.sleep(0.4)
assert all(ons)
print('STAGE28E_H_FINAL_FAKE_LOCKED_PASS sha=%s outputs=fake-locked rf_ready=0 shelly_master=on median_apower_w=%.2f samples=%s'%(E,statistics.median(vals),vals)); h.close()
PY
fi; echo STAGE28E_H_RECOVERY_RCS manual=$recovery_rc final=$final_rc; if [ \"$recovery_rc\" -ne 0 ] || [ \"$final_rc\" -ne 0 ]; then echo STAGE28E_H_RECOVERY_FAILURE; exit 99; fi; if [ \"$run_rc\" -ne 0 ]; then exit \"$run_rc\"; fi; echo STAGE28E_H_PHYSICAL_E2E_GATE_PASS",
    "set -euo pipefail; test -z \"$(git status --porcelain)\"; git diff --check; echo STAGE28E_H_CLEAN_TREE_PASS"
  ]
}
