{
  "id": "20260914-serial-monitor-policy-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "main",
  "allow_write": true,
  "resources": [],
  "command_timeout": 300,
  "task_timeout": 900,
  "memory_limit_mb": 2048,
  "commands": [
    "git fetch origin main && test \"$(git rev-parse HEAD)\" = \"b9e165f13841c8b4dc0f65eae77c5dc3e0e460eb\" && test -z \"$(git status --porcelain)\"",
    "python3 - <<'PY'\nfrom pathlib import Path\np=Path('Makefile')\ns=p.read_text()\ns=s.replace('        flash monitor flash-monitor menuconfig clean', '        flash monitor monitor-reset flash-monitor menuconfig clean')\ns=s.replace("\\t@printf '    make flash-monitor  — build + flash + monitor serial\\\\n'\\n\\t@printf '    make monitor        — monitor serial (bez buildu)\\\\n'", "\\t@printf '    make flash-monitor  — build + flash + monitor bez dodatkowego resetu\\\\n'\\n\\t@printf '    make monitor        — monitor serial bez resetu przy podłączeniu\\\\n'\\n\\t@printf '    make monitor-reset  — monitor serial z jawnym resetem przy podłączeniu\\\\n'\\n\\t@printf '                         W otwartym monitorze Ctrl+R = jawny restart płytki\\\\n'")\ns=s.replace('monitor: ensure-idf\\n\\t$(RUN_IDF) -B $(IDF_BUILD_DIR) $(IDF_PORT_ARGS) monitor\\n\\nflash-monitor: build\\n\\t$(RUN_IDF) -B $(IDF_BUILD_DIR) $(IDF_PORT_ARGS) flash monitor', 'monitor: ensure-idf\\n\\tESP_IDF_MONITOR_NO_RESET=1 $(RUN_IDF) -B $(IDF_BUILD_DIR) $(IDF_PORT_ARGS) monitor\\n\\nmonitor-reset: ensure-idf\\n\\t$(RUN_IDF) -B $(IDF_BUILD_DIR) $(IDF_PORT_ARGS) monitor\\n\\nflash-monitor: build\\n\\tESP_IDF_MONITOR_NO_RESET=1 $(RUN_IDF) -B $(IDF_BUILD_DIR) $(IDF_PORT_ARGS) flash monitor')\np.write_text(s)\n\nPath('.vscode/settings.json').write_text('{\\n  \\"idf.monitorNoReset\\": true\\n}\\n')\n\ndoc=Path('docs/ESP32_S3_SERIAL_PORT_RESET.md')\nd=doc.read_text()\nanchor='## Mandatory interpretation rule for future runtime tests\\n'\nsection='''## Repository monitor policy\\n\\nFor normal observation, connecting the monitor must not intentionally reset the board. The repository defaults are:\\n\\n- `make monitor` sets `ESP_IDF_MONITOR_NO_RESET=1`;\\n- the recommended VS Code ESP-IDF extension setting is `idf.monitorNoReset: true`;\\n- `make flash-monitor` flashes as requested but does not request an additional monitor-start reset;\\n- `make monitor-reset` is the explicit connect-and-reset mode;\\n- inside an already open ESP-IDF Monitor, `Ctrl+R` is the explicit target reset action.\\n\\nOn this exact Mac/CrowPanel path, a bounded hardware check on 2026-09-14 opened ESP-IDF Monitor 1.9.0 with `--no-reset` on `/dev/cu.usbserial-1130` without observing an ESP-ROM/POWERON boot marker. Raw pyserial opens are **not** equivalent: historical tests showed that pre-setting `DTR=False` and `RTS=False` was still able to reset this board.\\n\\nUse the no-reset monitor whenever boot continuity matters. Use the reset mode only when a restart is deliberately part of the operation. Flashing naturally remains a rebooting operation.\\n\\n'''
if section not in d:\n    d=d.replace(anchor, section+anchor)\ndoc.write_text(d)\n\ntest=Path('tests/test_serial_monitor_policy.py')\ntest.write_text('''import json\\nfrom pathlib import Path\\n\\n\\nROOT = Path(__file__).resolve().parents[1]\\n\\n\\ndef _make_target_body(makefile: str, target: str) -> str:\\n    marker = f"{target}:"\\n    start = makefile.index(marker)\\n    tail = makefile[start:]\\n    next_target = tail.find("\\n\\n", len(marker))\\n    return tail if next_target < 0 else tail[:next_target]\\n\\n\\ndef test_default_monitor_is_no_reset_and_reset_mode_is_explicit() -> None:\\n    makefile = (ROOT / "Makefile").read_text()\\n    monitor = _make_target_body(makefile, "monitor")\\n    monitor_reset = _make_target_body(makefile, "monitor-reset")\\n    flash_monitor = _make_target_body(makefile, "flash-monitor")\\n\\n    assert "ESP_IDF_MONITOR_NO_RESET=1" in monitor\\n    assert "ESP_IDF_MONITOR_NO_RESET=1" not in monitor_reset\\n    assert "ESP_IDF_MONITOR_NO_RESET=1" in flash_monitor\\n\\n\\ndef test_vscode_monitor_defaults_to_no_reset() -> None:\\n    settings = json.loads((ROOT / ".vscode/settings.json").read_text())\\n    assert settings["idf.monitorNoReset"] is True\\n''')\nPY",
    "python3 - <<'PY'\nimport json\nfrom pathlib import Path\nmakefile=Path('Makefile').read_text()\nassert 'monitor-reset: ensure-idf' in makefile\nassert makefile.count('ESP_IDF_MONITOR_NO_RESET=1') == 2\nassert json.loads(Path('.vscode/settings.json').read_text())['idf.monitorNoReset'] is True\nassert 'Ctrl+R' in Path('docs/ESP32_S3_SERIAL_PORT_RESET.md').read_text()\nprint('serial monitor policy assertions: OK')\nPY\n/opt/homebrew/bin/pre-commit run --all-files",
    "git diff --check && git status --short && git diff -- Makefile .vscode/settings.json docs/ESP32_S3_SERIAL_PORT_RESET.md tests/test_serial_monitor_policy.py",
    "git add Makefile .vscode/settings.json docs/ESP32_S3_SERIAL_PORT_RESET.md tests/test_serial_monitor_policy.py && git commit -m 'Make serial monitoring no-reset by default' && git push origin HEAD:main",
    "test -z \"$(git status --porcelain)\" && git rev-parse HEAD"
  ]
}
