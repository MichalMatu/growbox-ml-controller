{
  "id": "20260903-growbox-stage27c-chunk05-parse-context-v3",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": ["board:growbox-s3"],
  "command_timeout": 300,
  "idle_timeout": 300,
  "task_timeout": 600,
  "memory_limit_mb": 4096,
  "commands": [
    "set -euo pipefail; OUT=/tmp/stage27c-final-long-soak-chunk05-20260903-v1; test -f \"$OUT/raw-001.log\"; .venv/bin/python - \"$OUT/raw-001.log\" <<'PY'\nimport sys\nlines=open(sys.argv[1], encoding='utf-8', errors='replace').read().splitlines()\nidx=[i for i,l in enumerate(lines) if '(36989601)' in l]\nprint('MATCHES', len(idx))\nfor i in idx:\n    for j in range(max(0,i-2), min(len(lines),i+3)):\n        print(f'RAW_LINE_{j+1}', lines[j])\nassert len(idx)==1, idx\nPY\nprintf '%s\\n' '--- SOURCE MATCHES ---'; rg -n -C 6 'soak_v=2|climate_stage27' . --glob '!build/**' --glob '!.pio/**' --glob '!.git/**' | head -n 240"
  ]
}
