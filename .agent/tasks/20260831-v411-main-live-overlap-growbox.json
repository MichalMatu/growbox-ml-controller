{
  "id": "20260831-v411-main-live-overlap-growbox",
  "mode": "commands",
  "work_branch": "main",
  "allow_write": false,
  "resources": [],
  "memory_limit_mb": 128,
  "command_timeout": 90,
  "idle_timeout": 90,
  "task_timeout": 180,
  "commands": [
    "rm -f /tmp/local-agent-v411-main-growbox.ready; touch /tmp/local-agent-v411-main-growbox.ready; i=0; while [ \"$i\" -lt 300 ]; do [ -f /tmp/local-agent-v411-main-matrixhub.ready ] && exit 0; i=$((i+1)); sleep 0.1; done; echo 'matrixhub barrier not reached'; exit 7"
  ]
}
