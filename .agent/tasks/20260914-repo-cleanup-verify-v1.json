{
  "id": "20260914-repo-cleanup-verify-v1",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "main",
  "allow_write": true,
  "resources": [],
  "memory_limit_mb": 4096,
  "command_timeout": 900,
  "task_timeout": 1800,
  "commands": [
    "set -eu; git fetch --prune origin; test \"$(git rev-parse HEAD)\" = c77b750ba66d35784af0a1dce4763f0c1e9dc062; test \"$(git rev-parse origin/main)\" = c77b750ba66d35784af0a1dce4763f0c1e9dc062; git status --short; echo 'main exact SHA verified'",
    "set -eu; echo '=== stale live-doc references ==='; git grep -n -E 'ARCHITECTURE_HANDOFF|CONTINUATION_PLAN|FRESH_CHAT_BOOTSTRAP|EINK_(UI|SCD41).*(HANDOFF|PROMPT)|STAGE27.*HANDOFF|STAGE28E_PHASE_[A-H]_HANDOFF|OUTPUT_EXECUTION_ARCHITECTURE_.*AUDIT|OUTPUT_EXECUTION_IMPLEMENTATION_PLAN|PRESTAGE28D_GOLDEN_CHECKPOINT|QUALITY_FREEZE_PLAN|MVP_REBUILD_CLEANUP_PLAN|docs/GUIDANCE.md|INTEGRATION_CONVERGENCE' -- '*.md' || true",
    "set -eu; python3 - <<'PY'\nfrom pathlib import Path\nimport re, subprocess, sys\nfiles = subprocess.check_output(['git','ls-files','*.md'], text=True).splitlines()\nmissing=[]\npat=re.compile(r'\\[[^\\]]*\\]\\(([^)]+)\\)')\nfor name in files:\n    p=Path(name)\n    text=p.read_text(encoding='utf-8', errors='replace')\n    for raw in pat.findall(text):\n        target=raw.split('#',1)[0].strip()\n        if not target or target.startswith(('http://','https://','mailto:')):\n            continue\n        target=target.split(' ',1)[0]\n        q=(p.parent/target).resolve()\n        if not q.exists():\n            missing.append(f'{name}: {raw}')\nif missing:\n    print('Missing local markdown links:')\n    print('\\n'.join(missing))\n    sys.exit(1)\nprint(f'Local markdown links OK across {len(files)} tracked markdown files')\nPY",
    "set -eu; if [ -x .venv/bin/pre-commit ]; then .venv/bin/pre-commit run --all-files; else python3 -m pre_commit run --all-files; fi; echo '=== post-precommit status ==='; git status --short; git diff --check"
  ]
}
