{
  "id": "20260905-growbox-fan-path-audit-v2",
  "agent_binding": "815cf40f-8d2a-4e1f-b7cc-c0f4e37b6cb5",
  "mode": "commands",
  "work_branch": "mvp/environment-controller",
  "allow_write": false,
  "resources": [],
  "memory_limit_mb": 2048,
  "command_timeout": 300,
  "idle_timeout": 60,
  "task_timeout": 480,
  "workflow_policy": "efficient-verification-v1",
  "steps": [
    {
      "name": "fan-control-path-audit",
      "command": "set -euo pipefail; EXPECTED=87e700a5688051bdc6a95cad722b60334319f76d; git fetch -q origin mvp/environment-controller; test \"$(git rev-parse origin/mvp/environment-controller)\" = \"$EXPECTED\"; git reset --hard \"$EXPECTED\" >/dev/null; test \"$(git rev-parse HEAD)\" = \"$EXPECTED\"; printf '%s\\n' '=== FAN PATH HITS ==='; rg -n --hidden --glob '!.git/**' --glob '!build/**' --glob '!.pio/**' 'requested_fan|applied_fan|physical_fan|fan_physical|arbiter_dwell_holds|arbiter_transitions|on_threshold|min_off|force_fan|fan.*threshold|fan.*dwell|BinaryRoleArbiter|binary arbiter' . || true; printf '%s\\n' '=== RELEVANT FILES ==='; rg -l --hidden --glob '!.git/**' --glob '!build/**' --glob '!.pio/**' 'requested_fan|arbiter_dwell_holds|BinaryRoleArbiter|on_threshold|min_off' . | sort || true; printf '%s\\n' '=== TEST FILES ==='; find test -type f | grep -Ei '(climate|arbiter|environment|controller|actuat|fan)' | sort || true; test -z \"$(git status --porcelain)\"",
      "verification_level": "focused",
      "timeout": 300,
      "output_policy": "stream"
    }
  ]
}
