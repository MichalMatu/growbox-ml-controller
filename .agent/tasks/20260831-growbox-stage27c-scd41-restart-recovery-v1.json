{
  "id": "20260831-growbox-stage27c-scd41-restart-recovery-v1",
  "repository": "MichalMatu/growbox-ml-controller",
  "work_branch": "mvp/environment-controller",
  "allow_write": true,
  "command_timeout": 2400,
  "idle_timeout": 900,
  "task_timeout": 7200,
  "memory_limit_mb": 8192,
  "commands": [
    "git fetch origin mvp/environment-controller && git reset --hard origin/mvp/environment-controller && git clean -fd && test \"$(git rev-parse HEAD)\" = \"c8f231dbc1e9ca1c5eb6fc49346ee5d91b301cd1\" && test -e /dev/cu.usbserial-110",
    "set -euo pipefail; python3 - <<'PY'\nfrom pathlib import Path\n\np = Path('src/climate/native/Scd41InsideSource.cpp')\ns = p.read_text()\nold = '''  growbox_sensirion_i2c_bind_device(device_);\n  sensirion_i2c_hal_init();\n  scd4x_init(kScd41Address);\n  const int16_t error = scd4x_start_periodic_measurement();\n  if (error != 0) {\n    ESP_LOGW(kScd41Tag, \"start_periodic_measurement failed: %d\", static_cast<int>(error));\n  }\n'''\nnew = '''  growbox_sensirion_i2c_bind_device(device_);\n  sensirion_i2c_hal_init();\n  scd4x_init(kScd41Address);\n\n  // The SCD4x keeps measuring across an MCU-only reset. Stop any inherited\n  // periodic session first; the upstream call includes the required 500 ms\n  // settling delay when the stop command is accepted. An idle sensor may NACK\n  // the stop command, in which case starting a fresh session is still valid.\n  const int16_t stop_error = scd4x_stop_periodic_measurement();\n  if (stop_error != 0) {\n    ESP_LOGI(kScd41Tag, \"pre-start stop_periodic_measurement returned %d; continuing\",\n             static_cast<int>(stop_error));\n  }\n\n  const int16_t error = scd4x_start_periodic_measurement();\n  if (error != 0) {\n    ESP_LOGW(kScd41Tag, \"start_periodic_measurement failed: %d\", static_cast<int>(error));\n  }\n'''\nassert old in s\np.write_text(s.replace(old, new))\nPY\n.venv/bin/pre-commit run --files src/climate/native/Scd41InsideSource.cpp && git diff --check",
    "bash scripts/stage27c_crowpanel.sh clean && bash scripts/stage27c_crowpanel.sh build",
    "set -euo pipefail; PORT=/dev/cu.usbserial-110 bash scripts/stage27c_crowpanel.sh flash; .venv/bin/python - <<'PY' | tee /tmp/stage27c-scd41-recovery.log\nimport serial\nimport time\n\nport = '/dev/cu.usbserial-110'\ndeadline = time.monotonic() + 90.0\nprint(f'SERIAL_CAPTURE_BEGIN port={port} seconds=90', flush=True)\nwith serial.Serial(port, 115200, timeout=1.0) as ser:\n    while time.monotonic() < deadline:\n        raw = ser.readline()\n        if raw:\n            line = raw.decode('utf-8', errors='replace').rstrip()\n            if 'climate_stage27' in line or 'scd41' in line or 'NimBLE' in line:\n                print(line, flush=True)\nprint('SERIAL_CAPTURE_END', flush=True)\nPY\ngrep -q 'I2C probe: scd41_0x62=ESP_OK ds3231_0x68=ESP_OK' /tmp/stage27c-scd41-recovery.log\ngrep -q 'Stage27 real-input runtime: i2c=1 scd41=1 ds3231=1 ble=1 outputs=fake-locked' /tmp/stage27c-scd41-recovery.log\ngrep -q 'scd41_sample=1' /tmp/stage27c-scd41-recovery.log\ngrep -q 'rtc_available=1 rtc_trusted=1' /tmp/stage27c-scd41-recovery.log\ngrep -Eq 'ble_last_valid_ms=[1-9][0-9]*' /tmp/stage27c-scd41-recovery.log",
    "git add src/climate/native/Scd41InsideSource.cpp && git diff --cached --check && git commit -m 'Recover SCD41 after MCU-only reset' && git push origin HEAD:mvp/environment-controller && git status --short && git rev-parse HEAD"
  ]
}
