#!/usr/bin/env python3
from __future__ import annotations

import json
from pathlib import Path

ROOT = Path('.')

model_pipeline = ROOT / 'docs/MODEL_PIPELINE.md'
model_pipeline.write_text('''# Model pipeline\n\nStatus: climate-v6 research path qualified through Stage 16; Rule policy remains the recommended runtime authority until a later ML acceptance gate.\n\nThe active climate path is hardware-independent and keeps policy selection separate from arbitration and `SafetySupervisor`. The current ML contract has 44 ordered input features and 6 climate outputs. Light and circulation fan remain deterministic product behavior outside ML v1.\n\n## Active architecture\n\n```text\nEnvironmentState + targets + measurement status\n                |\n                v\n      climate-v6 feature encoder (44)\n                |\n        +-------+-------+\n        |               |\n        v               v\nClimateRulePolicy   ML candidate / shadow\n        |               |\n        +-------+-------+\n                |\n                v\n           arbitration\n                |\n                v\n        SafetySupervisor\n                |\n                v\n        applied safe action\n                |\n                v\n effective-actuator estimator\n```\n\nThe six ML outputs are:\n\n- `heater`\n- `cooler`\n- `exhaust_fan`\n- `humidifier`\n- `dehumidifier`\n- `co2_doser`\n\nThe six Stage-13 effective-state features are the estimated applied states for the same actuators. They are advanced from the safe/applied semantic command after each interval, never from raw ML output.\n\n## Current model shape\n\nThe bounded MLP remains:\n\n```text\n44 -> 32 -> 32 -> 6\n```\n\nwith 2,694 trainable parameters. Stage 11 showed that widening to 64 hidden units worsened closed-loop behavior, so width expansion is not an active optimization direction.\n\n`reports/ml/climate_v6_model_stage13_compat.*` is the compatibility artifact for the Stage-13 feature contract. It preserves the earlier model exactly by inserting the six effective-state inputs with zero first-layer weights; it was not retrained on the new features.\n\n## Teacher and data policy\n\nThe old single-step/repeated-action rollout teacher is retained only for compatibility. New labels must use the stronger Sequence Teacher explicitly. `tools/ml/climate_dagger.py` therefore keeps `teacher_kind=\"rollout\"` as the legacy default but supports explicit `teacher_kind=\"sequence\"`; no new experiment may silently generate old-Teacher data.\n\nSequence Teacher uses a 300 s receding-horizon plan and was qualified on untouched Stage-12 final evaluation before later research stages.\n\n## Acceptance history\n\nThe authoritative detailed record is [ML_DECISION_REPORT.md](ML_DECISION_REPORT.md). Current decisions are:\n\n- Stage 12: Sequence Teacher qualified.\n- Stage 13: effective actuator observability implemented and fully verified.\n- Stage 14: bounded residual ML rejected; tracking gains were too small relative to switching cost.\n- Stage 15: deterministic CO2/exhaust coupling edits rejected on representative DEV.\n- Stage 16: one bounded Sequence-Teacher DAgger iteration improved the current ML candidate on DEV but failed the switching gate on two DEV seeds and remained worse than Rule. No second DAgger iteration and no candidate publication.\n\n## Runtime direction\n\nThe next runtime work is deliberately product-oriented rather than another training sweep:\n\n1. keep `ClimateRulePolicy` authoritative;\n2. add explicit `Rule`, `MlShadow`, and gated `MlActive` policy modes;\n3. allow shadow ML to produce diagnostics without affecting actuators;\n4. record deterministic controller traces containing measurements, targets, encoded features, Rule request, ML-shadow request, arbitration, safety reasons, applied action, and effective-state estimate;\n5. add replay and Python/C++ parity before real-sensor integration;\n6. calibrate simulator dynamics from hardware later, then reconsider ML architecture only with new evidence.\n\n## Verification rule\n\nClosed-loop behavior is the primary model gate. Offline MAE/F1 may diagnose training, but they do not qualify a controller. `SafetySupervisor` remains authoritative for Rule, teacher, shadow, or active ML paths.\n''')

report_md = ROOT / 'docs/ML_DECISION_REPORT.md'
if report_md.exists():
    raise SystemExit('docs/ML_DECISION_REPORT.md already exists')
report_md.write_text('''# ML decision report — climate-v6 Stages 12–16\n\nDate: 2026-08-28  \nBranch: `mvp/environment-controller`\n\nThis document freezes the decisions reached by the climate-v6 research program so later runtime/product work does not repeat rejected experiments or accidentally reuse revealed evaluation seeds.\n\n## Executive decision\n\n`ClimateRulePolicy` remains the recommended authoritative runtime controller. ML remains valuable as an experimental/shadow policy, but no trained ML candidate from Stages 12–16 is qualified to replace Rule for active actuation. `SafetySupervisor` remains authoritative regardless of policy mode.\n\n## Stage 12 — stronger Sequence Teacher\n\nThe previous teacher was too weak because it repeatedly applied the same action over the prediction horizon and did not observe hidden actuator lag state. A receding-horizon Sequence Teacher was added.\n\nUntouched final seed `173205`:\n\n- Rule tracking: `3.91479117413`\n- Sequence Teacher tracking: `3.73843104661`\n- tracking improvement: `4.5049689672%`\n- Rule switching: `0.00507047`\n- Sequence switching: `0.0215463`\n- safety interventions: `0`\n- hard-limit violations: `0`\n\nVerdict: **PASS**. Sequence Teacher is the required teacher for new climate labels.\n\n## Stage 13 — effective actuator observability\n\nSix estimated effective actuator states were added to the feature contract:\n\n- heater, tau 35 s\n- cooler, tau 45 s\n- exhaust fan, tau 8 s\n- humidifier, tau 20 s\n- dehumidifier, tau 20 s\n- CO2 doser, immediate response in the estimator\n\nThe estimator advances from the safe/applied semantic action after the interval. It never consumes raw ML requests. The input contract therefore moved from 38 to 44 features while keeping 6 outputs.\n\nCurrent bounded network: `44 -> 32 -> 32 -> 6`, 2,694 parameters.\n\nImplementation commit: `34d164cd7b5a68ed7b46228e7aef8539c7539c6e`. Full verification passed (Python, schema, host C++, clang-tidy).\n\nVerdict: **KEEP**. This is part of the active climate-v6 contract.\n\n## Stage 14 — residual ML\n\nResidual control was evaluated as `u = clamp(u_rule + delta_u_ml)`. Projection removed unsafe/opposed commands, but representative DEV did not produce an acceptable tracking/switching Pareto point.\n\nThe larger 180-row data probe still showed:\n\n- all-output residual tracking gain: `0.89356%`\n- all-output switching increase: `50.84%`\n- cooler+exhaust tracking gain: `0.71349%`\n- cooler+exhaust switching increase: `35.34%`\n\nVerdict: **NO-GO**. Do not implement residual policy in runtime from this evidence. Do not widen the MLP to compensate.\n\n## Stage 15 — CO2/exhaust deterministic coupling\n\nCO2 dosing and exhaust are physically coupled, but simple deterministic suppression/prioritization rules regressed representative DEV behavior.\n\nThe dominant-request candidate on DEV seed `316227` produced:\n\n- tracking: `0.30985%` worse than Rule\n- switching: `71.8%` higher\n- CO2 MAE: `8.4278%` worse\n- safety/arbitration/hard-limit violations: zero after the candidate rule itself\n\nVerdict: **NO-GO**. Do not add a generic CO2/exhaust coupling heuristic without a new physical/runtime hypothesis.\n\n## Stage 16 — limited Sequence-Teacher DAgger\n\nDAgger was updated so new rows can explicitly use Sequence Teacher while preserving the legacy rollout default for compatibility. Source commit: `698d4e7e31a192a19b9f18cc0d11ea7a1a146fa6`.\n\nExactly one bounded Sequence-Teacher iteration was tested and confirmed on a second DEV seed. The candidate was never persisted.\n\nDEV seed `282843`:\n\n- current ML tracking: `5.23519034206`\n- candidate tracking: `4.94940628142`\n- improvement vs current ML: `5.4589048719%`\n- Rule tracking: `4.25717215776`\n- switching ratio vs current ML: `1.5622759317`\n- candidate arbitration/safety/hard-limit fractions: `0 / 0 / 0`\n- iteration-2 gate: `false`\n\nConfirmation DEV seed `331643`:\n\n- current ML tracking: `5.98614583203`\n- candidate tracking: `5.84959558030`\n- improvement vs current ML: `2.2811046633%`\n- Rule tracking: `4.99485892680`\n- switching ratio vs current ML: `2.0542329056`\n- candidate arbitration/safety/hard-limit fractions: `0 / 0 / 0`\n- iteration-2 gate: `false`\n\nVerdict: **STOP after one iteration**. The candidate consistently improves the current ML model, but switching cost is unacceptable and Rule remains better. No iteration 2, no final/test inspection for this candidate, and no publication.\n\n## Seed hygiene\n\nRevealed/burned seeds must not be reused as untouched final seeds:\n\n`271828, 314159, 577215, 424242, 91273, 161803, 173205, 141421, 223607, 244949, 316227, 707107, 282843, 331643`\n\nStage-14 A/B reserved test metrics remain uninspected.\n\n## Frozen next direction\n\nDo not start another broad training sweep from the same synthetic assumptions. The next product stages are:\n\n1. runtime policy modes: `Rule`, `MlShadow`, gated `MlActive`;\n2. shadow diagnostics while Rule remains authoritative;\n3. trace recording and deterministic replay;\n4. Python/C++ runtime parity;\n5. fake/simulator adapters and hardware-in-the-loop preparation;\n6. later hardware calibration of actuator/sensor dynamics;\n7. ML v2 only after real or calibrated trajectories provide a new hypothesis.\n''')

report = {
    'date': '2026-08-28',
    'branch': 'mvp/environment-controller',
    'runtime_authority': 'rule',
    'ml_active_qualified': False,
    'contract': {'features': 44, 'outputs': 6, 'hidden_width': 32, 'parameters': 2694},
    'source_heads': {
        'effective_observability': '34d164cd7b5a68ed7b46228e7aef8539c7539c6e',
        'sequence_dagger_injection': '698d4e7e31a192a19b9f18cc0d11ea7a1a146fa6',
    },
    'decisions': {
        'stage12_sequence_teacher': 'pass',
        'stage13_effective_observability': 'keep',
        'stage14_residual_policy': 'no_go',
        'stage15_deterministic_co2_exhaust_coupling': 'no_go',
        'stage16_sequence_dagger_iteration2': 'stop',
    },
    'burned_seeds': [271828, 314159, 577215, 424242, 91273, 161803, 173205, 141421, 223607, 244949, 316227, 707107, 282843, 331643],
    'reserved_stage14ab_test_metrics_evaluated': False,
    'next_stages': ['runtime_policy_modes', 'shadow_diagnostics', 'trace_and_replay', 'python_cpp_parity', 'simulated_hil', 'hardware_calibration_later'],
}
(ROOT / 'docs/ML_DECISION_REPORT.json').write_text(json.dumps(report, indent=2, sort_keys=True) + '\n')

mvp = ROOT / 'docs/MVP_ENVIRONMENT_CONTROLLER.md'
text = mvp.read_text()
old_status = 'Status: architecture/design phase — MVP v1 flow, I/O and rebuild plan frozen'
new_status = 'Status: implementation phase — climate-v6 simulation/training path qualified through Stage 16; hardware runtime integration remains in progress'
if old_status not in text:
    raise SystemExit('expected MVP status line not found')
text = text.replace(old_status, new_status, 1)
marker = '# Frozen implementation plan\n'
checkpoint = '''## Current implementation checkpoint — 2026-08-28\n\nThe rebuild has progressed beyond the original planning checkpoint:\n\n- climate-v6 uses 44 ordered ML features and 6 climate outputs;\n- Sequence Teacher is qualified for new labels;\n- effective actuator observability is implemented;\n- Rule remains the recommended authoritative runtime policy;\n- residual ML and deterministic CO2/exhaust coupling were rejected on DEV evidence;\n- one bounded Sequence-Teacher DAgger iteration failed the switching gate on two DEV seeds, so no second iteration or candidate publication is planned;\n- next work is runtime policy-mode/shadow diagnostics, trace/replay, parity, and simulated/HIL preparation before real sensors.\n\nSee `docs/ML_DECISION_REPORT.md` for the frozen evidence and seed hygiene.\n\n'''
if checkpoint not in text:
    if marker not in text:
        raise SystemExit('frozen implementation marker not found')
    text = text.replace(marker, checkpoint + marker, 1)
mvp.write_text(text)

changelog = ROOT / 'docs/CHANGELOG.md'
text = changelog.read_text()
anchor = '## Unreleased\n\n'
addition = '''- Complete climate-v6 research through Stage 16: qualify Sequence Teacher, add six effective-actuator state features, and preserve authoritative safety after applied actions.\n- Keep the bounded 44 -> 32 -> 32 -> 6 MLP; reject residual policy and simple deterministic CO2/exhaust coupling after representative DEV regressions/trade-offs.\n- Add explicit Sequence-Teacher DAgger support while preserving the legacy rollout default; stop after one bounded DEV iteration because switching gates fail on two DEV seeds.\n- Freeze ML decisions and seed hygiene in `docs/ML_DECISION_REPORT.md`; move the next milestone to Rule-authoritative runtime shadow diagnostics and deterministic trace/replay.\n'''
if addition not in text:
    if anchor not in text:
        raise SystemExit('Unreleased changelog anchor not found')
    text = text.replace(anchor, anchor + addition, 1)
changelog.write_text(text)
