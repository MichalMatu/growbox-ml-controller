# Growbox continuation pointer

This root-level file is retained only as a compatibility pointer for older links and handoffs. It is **not** the current project handoff and must not be used as the source of truth for active work.

For a fresh conversation or continuation, read in this order:

1. `AGENTS.md`
2. `docs/FRESH_CHAT_BOOTSTRAP.md`
3. `docs/CURRENT_STATUS.md`
4. `docs/ARCHITECTURE.md`
5. `docs/PROJECT_ROADMAP.md`
6. `docs/CONTINUATION_PLAN.md`

The active development branch is `main`; the Local Agent control branch is `agent-control`.

Before any write or Local Agent task, fetch the fresh `main` HEAD and `agent-control:.agent/status/daemon.json` and work from repository evidence rather than remembered chat state.

Historical Stage27/Stage28 handoffs, qualification records, and superseded branch instructions remain available under `docs/` and in Git history as evidence. Do not reinterpret them as the current development sequence or as qualification for newer executable SHAs.
