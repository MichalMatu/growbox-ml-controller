{
  "name": "growbox-hardware-configurator",
  "private": true,
  "version": "0.0.0",
  "description": "Sparse FE line: contract v4 docs, root golden gate, and web/ hardware configurator SPA.",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "gate": "node gate/check-contract.mjs && pnpm --dir web gate",
    "check": "pnpm gate && pnpm test:contract",
    "test:contract": "node --test gate/check-contract.test.mjs"
  }
}
